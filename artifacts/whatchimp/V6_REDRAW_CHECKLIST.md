# Oraya Natural Stay Intake v6 — operator redraw checklist (hybrid import repair)

**Machine-generated by `scripts/generate-whatchimp-v6.mjs` — do not edit by hand; it is regenerated with the artifact and verified against it by an exact set-equality assertion.**

**Applies to exactly:** `Oraya_natural_intake_v6.txt` with SHA-256 `1C7335ED49C7F9F7738B58CCDA4BA340543A9354002FAC2F9FBEC672784EDD48`. If your file's hash differs, regenerate this checklist.

**Why this exists:** authenticated round trip #1 (2026-07-03) proved WhatChimp's import keeps only the FIRST serialized connection per input socket and silently drops the rest (evidence: `roundtrips/ROUNDTRIP_1_FINDINGS.md`). Round trip #3 proved a Condition accepts **one inbound connection TOTAL, of any source type** (evidence: `roundtrips/ROUNDTRIP_3_FINDINGS.md`), so this candidate clones every branch-local tail AND every Condition that would otherwise receive a second inbound connection — the 39 remaining merge connections below are exactly what you re-draw after import, and **none of them targets a Condition**. Every item uses only a live-proven editor operation (Condition → question flow, Text → question flow, Condition → Text); the generator refuses to emit this file otherwise. If the editor rejects ANY connection below, stop and report — do not insert nodes to work around it.

**When:** on a fresh disposable TEST bot, immediately after importing the artifact and BEFORE any testing: draw all connections below, then Save → close the editor → reopen → export → verify (commands at the bottom).

**Safety of the un-repaired import** (until you finish drawing): the complete happy path already works (all details extracted → bedroom OK → confirmation → handoff); no message ever claims a confirmed booking; the opening question always shows `https://stayoraya.com/book` before anything else, so no conversation is ever link-less. But the not-yet-drawn branches stop silently — never expose the bot to guests before the redraw + verification below. Production stays untouched.

## The 39 connections to draw

Each item: drag a line from the stated OUTPUT of the source node to the (single) INPUT socket of the destination node. Canvas coordinates are from the import file — nodes may shift slightly, so match by the quoted title/text.

- [ ] **1.** FROM `#750` Condition diamond (oraya_guest_count = "null") — **TRUE output** (`conditionOutputTrue`)
      TO `#600` Flow wrapper "Oraya v6 - Guests" — input socket (`userInputFlowInput`).
      Purpose: Dates recovered after the FIRST full-dates follow-up, guest count still unknown — asks the guest question.
      Where: source at canvas ≈ (4345, -1533); destination at ≈ (5200, -1900) (above and right of the source).
      Operation: Condition output → User Input Flow input — live-proven editor operation (2026-07-03).
- [ ] **2.** FROM `#752` Condition diamond (oraya_guest_count = "null") — **TRUE output** (`conditionOutputTrue`)
      TO `#600` Flow wrapper "Oraya v6 - Guests" — input socket (`userInputFlowInput`).
      Purpose: Dates recovered after the SECOND full-dates follow-up, guest count still unknown — asks the guest question.
      Where: source at canvas ≈ (4722, -2622); destination at ≈ (5200, -1900) (below and right of the source).
      Operation: Condition output → User Input Flow input — live-proven editor operation (2026-07-03).
- [ ] **3.** FROM `#754` Condition diamond (oraya_guest_count = "null") — **TRUE output** (`conditionOutputTrue`)
      TO `#600` Flow wrapper "Oraya v6 - Guests" — input socket (`userInputFlowInput`).
      Purpose: Check-out recovered after the FIRST check-out follow-up, guest count still unknown — asks the guest question.
      Where: source at canvas ≈ (4273, -965); destination at ≈ (5200, -1900) (above and right of the source).
      Operation: Condition output → User Input Flow input — live-proven editor operation (2026-07-03).
- [ ] **4.** FROM `#756` Condition diamond (oraya_guest_count = "null") — **TRUE output** (`conditionOutputTrue`)
      TO `#600` Flow wrapper "Oraya v6 - Guests" — input socket (`userInputFlowInput`).
      Purpose: Check-out recovered after the SECOND check-out follow-up, guest count still unknown — asks the guest question.
      Where: source at canvas ≈ (4549, 575); destination at ≈ (5200, -1900) (above and right of the source).
      Operation: Condition output → User Input Flow input — live-proven editor operation (2026-07-03).
- [ ] **5.** FROM `#751` Condition diamond (oraya_guest_count = "1" OR oraya_guest_count = "2" OR oraya_guest_count = "3" OR oraya_guest_count = "4" OR oraya_guest_count = "5" OR oraya_guest_count = "6" OR oraya_guest_count = "7" OR oraya_guest_count = "8") — **TRUE output** (`conditionOutputTrue`)
      TO `#610` Flow wrapper "Oraya v6 - Bedrooms" — input socket (`userInputFlowInput`).
      Purpose: First full-dates recovery with a known, supported guest count — continues to the bedroom question.
      Where: source at canvas ≈ (4605, -1533); destination at ≈ (5200, -1600) (above and right of the source).
      Operation: Condition output → User Input Flow input — live-proven editor operation (2026-07-03).
- [ ] **6.** FROM `#753` Condition diamond (oraya_guest_count = "1" OR oraya_guest_count = "2" OR oraya_guest_count = "3" OR oraya_guest_count = "4" OR oraya_guest_count = "5" OR oraya_guest_count = "6" OR oraya_guest_count = "7" OR oraya_guest_count = "8") — **TRUE output** (`conditionOutputTrue`)
      TO `#610` Flow wrapper "Oraya v6 - Bedrooms" — input socket (`userInputFlowInput`).
      Purpose: Second full-dates recovery with a known, supported guest count — continues to the bedroom question.
      Where: source at canvas ≈ (4982, -2622); destination at ≈ (5200, -1600) (below and right of the source).
      Operation: Condition output → User Input Flow input — live-proven editor operation (2026-07-03).
- [ ] **7.** FROM `#755` Condition diamond (oraya_guest_count = "1" OR oraya_guest_count = "2" OR oraya_guest_count = "3" OR oraya_guest_count = "4" OR oraya_guest_count = "5" OR oraya_guest_count = "6" OR oraya_guest_count = "7" OR oraya_guest_count = "8") — **TRUE output** (`conditionOutputTrue`)
      TO `#610` Flow wrapper "Oraya v6 - Bedrooms" — input socket (`userInputFlowInput`).
      Purpose: First check-out recovery with a known, supported guest count — continues to the bedroom question.
      Where: source at canvas ≈ (4533, -965); destination at ≈ (5200, -1600) (above and right of the source).
      Operation: Condition output → User Input Flow input — live-proven editor operation (2026-07-03).
- [ ] **8.** FROM `#757` Condition diamond (oraya_guest_count = "1" OR oraya_guest_count = "2" OR oraya_guest_count = "3" OR oraya_guest_count = "4" OR oraya_guest_count = "5" OR oraya_guest_count = "6" OR oraya_guest_count = "7" OR oraya_guest_count = "8") — **TRUE output** (`conditionOutputTrue`)
      TO `#610` Flow wrapper "Oraya v6 - Bedrooms" — input socket (`userInputFlowInput`).
      Purpose: Second check-out recovery with a known, supported guest count — continues to the bedroom question.
      Where: source at canvas ≈ (4809, 575); destination at ≈ (5200, -1600) (above and right of the source).
      Operation: Condition output → User Input Flow input — live-proven editor operation (2026-07-03).
- [ ] **9.** FROM `#766` Condition diamond (oraya_guest_count = "1" OR oraya_guest_count = "2" OR oraya_guest_count = "3" OR oraya_guest_count = "4" OR oraya_guest_count = "5" OR oraya_guest_count = "6" OR oraya_guest_count = "7" OR oraya_guest_count = "8") — **TRUE output** (`conditionOutputTrue`)
      TO `#610` Flow wrapper "Oraya v6 - Bedrooms" — input socket (`userInputFlowInput`).
      Purpose: Guest count the guest JUST answered is supported (1–8) — continues to the bedroom question.
      Where: source at canvas ≈ (5850, -1780); destination at ≈ (5200, -1600) (below and left of the source).
      Operation: Condition output → User Input Flow input — live-proven editor operation (2026-07-03).
- [ ] **10.** FROM `#751` Condition diamond (oraya_guest_count = "1" OR oraya_guest_count = "2" OR oraya_guest_count = "3" OR oraya_guest_count = "4" OR oraya_guest_count = "5" OR oraya_guest_count = "6" OR oraya_guest_count = "7" OR oraya_guest_count = "8") — **FALSE output** (`conditionOutputFalse`)
      TO `#466` Flow wrapper "Oraya v6 - Guests 8+" — input socket (`userInputFlowInput`).
      Purpose: First full-dates recovery with more than 8 guests — exact-count team review.
      Where: source at canvas ≈ (4605, -1533); destination at ≈ (5451, -2777) (above and right of the source).
      Operation: Condition output → User Input Flow input — live-proven editor operation (2026-07-03).
- [ ] **11.** FROM `#753` Condition diamond (oraya_guest_count = "1" OR oraya_guest_count = "2" OR oraya_guest_count = "3" OR oraya_guest_count = "4" OR oraya_guest_count = "5" OR oraya_guest_count = "6" OR oraya_guest_count = "7" OR oraya_guest_count = "8") — **FALSE output** (`conditionOutputFalse`)
      TO `#466` Flow wrapper "Oraya v6 - Guests 8+" — input socket (`userInputFlowInput`).
      Purpose: Second full-dates recovery with more than 8 guests — exact-count team review.
      Where: source at canvas ≈ (4982, -2622); destination at ≈ (5451, -2777) (above and right of the source).
      Operation: Condition output → User Input Flow input — live-proven editor operation (2026-07-03).
- [ ] **12.** FROM `#755` Condition diamond (oraya_guest_count = "1" OR oraya_guest_count = "2" OR oraya_guest_count = "3" OR oraya_guest_count = "4" OR oraya_guest_count = "5" OR oraya_guest_count = "6" OR oraya_guest_count = "7" OR oraya_guest_count = "8") — **FALSE output** (`conditionOutputFalse`)
      TO `#466` Flow wrapper "Oraya v6 - Guests 8+" — input socket (`userInputFlowInput`).
      Purpose: First check-out recovery with more than 8 guests — exact-count team review.
      Where: source at canvas ≈ (4533, -965); destination at ≈ (5451, -2777) (above and right of the source).
      Operation: Condition output → User Input Flow input — live-proven editor operation (2026-07-03).
- [ ] **13.** FROM `#757` Condition diamond (oraya_guest_count = "1" OR oraya_guest_count = "2" OR oraya_guest_count = "3" OR oraya_guest_count = "4" OR oraya_guest_count = "5" OR oraya_guest_count = "6" OR oraya_guest_count = "7" OR oraya_guest_count = "8") — **FALSE output** (`conditionOutputFalse`)
      TO `#466` Flow wrapper "Oraya v6 - Guests 8+" — input socket (`userInputFlowInput`).
      Purpose: Second check-out recovery with more than 8 guests — exact-count team review.
      Where: source at canvas ≈ (4809, 575); destination at ≈ (5451, -2777) (above and right of the source).
      Operation: Condition output → User Input Flow input — live-proven editor operation (2026-07-03).
- [ ] **14.** FROM `#766` Condition diamond (oraya_guest_count = "1" OR oraya_guest_count = "2" OR oraya_guest_count = "3" OR oraya_guest_count = "4" OR oraya_guest_count = "5" OR oraya_guest_count = "6" OR oraya_guest_count = "7" OR oraya_guest_count = "8") — **FALSE output** (`conditionOutputFalse`)
      TO `#466` Flow wrapper "Oraya v6 - Guests 8+" — input socket (`userInputFlowInput`).
      Purpose: Guest count the guest JUST answered is more than 8 — exact-count team review.
      Where: source at canvas ≈ (5850, -1780); destination at ≈ (5451, -2777) (above and left of the source).
      Operation: Condition output → User Input Flow input — live-proven editor operation (2026-07-03).
- [ ] **15.** FROM `#758` Condition diamond (oraya_villa = "null") — **TRUE output** (`conditionOutputTrue`)
      TO `#480` Flow wrapper "Oraya v6 - Villa" — input socket (`userInputFlowInput`).
      Purpose: Bedroom OK — 2 bedrooms fit 3–4 guests (first ask) — villa still unknown, asks the villa question.
      Where: source at canvas ≈ (6500, -1480); destination at ≈ (6641, -1546) (above and right of the source).
      Operation: Condition output → User Input Flow input — live-proven editor operation (2026-07-03).
- [ ] **16.** FROM `#759` Condition diamond (oraya_villa = "null") — **TRUE output** (`conditionOutputTrue`)
      TO `#480` Flow wrapper "Oraya v6 - Villa" — input socket (`userInputFlowInput`).
      Purpose: Bedroom OK — 3 bedrooms or 1–2 guests (retry ask) — villa still unknown, asks the villa question.
      Where: source at canvas ≈ (5980, -1180); destination at ≈ (6641, -1546) (above and right of the source).
      Operation: Condition output → User Input Flow input — live-proven editor operation (2026-07-03).
- [ ] **17.** FROM `#760` Condition diamond (oraya_villa = "null") — **TRUE output** (`conditionOutputTrue`)
      TO `#480` Flow wrapper "Oraya v6 - Villa" — input socket (`userInputFlowInput`).
      Purpose: Bedroom OK — 2 bedrooms fit 3–4 guests (retry ask) — villa still unknown, asks the villa question.
      Where: source at canvas ≈ (6500, -1180); destination at ≈ (6641, -1546) (above and right of the source).
      Operation: Condition output → User Input Flow input — live-proven editor operation (2026-07-03).
- [ ] **18.** FROM `#614` Condition diamond (oraya_guest_count = "3" OR oraya_guest_count = "4") — **FALSE output** (`conditionOutputFalse`)
      TO `#616` Message "That bedroom setup won’t quite fit #oraya_guest_count# overnight guest…" — input socket (`textInput`).
      Purpose: Bedroom mismatch — 2 bedrooms with 5–8 guests (first ask) — shows the capacity explanation and re-ask.
      Where: source at canvas ≈ (6240, -1600); destination at ≈ (6760, -1600) (level with and right of the source).
      Operation: Condition output → Text input — live-proven editor operation (2026-07-03).
- [ ] **19.** FROM `#758` Condition diamond (oraya_villa = "null") — **FALSE output** (`conditionOutputFalse`)
      TO `#490` Flow wrapper "Oraya v6 - Confirm" — input socket (`userInputFlowInput`).
      Purpose: Bedroom OK (first ask), villa already known — continues to the confirmation summary.
      Where: source at canvas ≈ (6500, -1480); destination at ≈ (6820, -933) (below and right of the source).
      Operation: Condition output → User Input Flow input — live-proven editor operation (2026-07-03).
- [ ] **20.** FROM `#759` Condition diamond (oraya_villa = "null") — **FALSE output** (`conditionOutputFalse`)
      TO `#490` Flow wrapper "Oraya v6 - Confirm" — input socket (`userInputFlowInput`).
      Purpose: Bedroom OK (retry, 3 bedrooms or 1–2 guests), villa already known — continues to the confirmation summary.
      Where: source at canvas ≈ (5980, -1180); destination at ≈ (6820, -933) (below and right of the source).
      Operation: Condition output → User Input Flow input — live-proven editor operation (2026-07-03).
- [ ] **21.** FROM `#760` Condition diamond (oraya_villa = "null") — **FALSE output** (`conditionOutputFalse`)
      TO `#490` Flow wrapper "Oraya v6 - Confirm" — input socket (`userInputFlowInput`).
      Purpose: Bedroom OK (retry, 2 bedrooms fit 3–4), villa already known — continues to the confirmation summary.
      Where: source at canvas ≈ (6500, -1180); destination at ≈ (6820, -933) (below and right of the source).
      Operation: Condition output → User Input Flow input — live-proven editor operation (2026-07-03).
- [ ] **22.** FROM `#604` Message "Lovely choice 😊" — **output** (`textOutput`)
      TO `#490` Flow wrapper "Oraya v6 - Confirm" — input socket (`userInputFlowInput`).
      Purpose: Villa the guest JUST chose continues to the confirmation summary (the villa-already-known path is kept automatically).
      Where: source at canvas ≈ (7190, -1750); destination at ≈ (6820, -933) (below and left of the source).
      Operation: Text output → User Input Flow input — live-proven editor operation (2026-07-03).
- [ ] **23.** FROM `#767` Condition diamond (oraya_check_in = "null" OR oraya_check_out = "null") — **TRUE output** (`conditionOutputTrue`)
      TO `#655` Message "I’m having a little trouble reading the dates — let me bring in our te…" — input socket (`textInput`).
      Purpose: Edit check-out refinement still left dates incomplete — date-trouble escalation message.
      Where: source at canvas ≈ (3500, -160); destination at ≈ (3500, -520) (above and near the source).
      Operation: Condition output → Text input — live-proven editor operation (2026-07-03).
- [ ] **24.** FROM `#761` Condition diamond (oraya_guest_count = "null") — **TRUE output** (`conditionOutputTrue`)
      TO `#661` Flow wrapper "Oraya v6 Edit - Guests" — input socket (`userInputFlowInput`).
      Purpose: Edit attempt that already has BOTH dates, guest count unknown — asks the Edit guest question.
      Where: source at canvas ≈ (2720, -160); destination at ≈ (4020, -520) (above and right of the source).
      Operation: Condition output → User Input Flow input — live-proven editor operation (2026-07-03).
- [ ] **25.** FROM `#768` Condition diamond (oraya_guest_count = "null") — **TRUE output** (`conditionOutputTrue`)
      TO `#661` Flow wrapper "Oraya v6 Edit - Guests" — input socket (`userInputFlowInput`).
      Purpose: Edit check-out refinement recovered the dates, guest count unknown — asks the Edit guest question.
      Where: source at canvas ≈ (3760, -160); destination at ≈ (4020, -520) (above and right of the source).
      Operation: Condition output → User Input Flow input — live-proven editor operation (2026-07-03).
- [ ] **26.** FROM `#762` Condition diamond (oraya_guest_count = "1" OR oraya_guest_count = "2" OR oraya_guest_count = "3" OR oraya_guest_count = "4" OR oraya_guest_count = "5" OR oraya_guest_count = "6" OR oraya_guest_count = "7" OR oraya_guest_count = "8") — **TRUE output** (`conditionOutputTrue`)
      TO `#670` Flow wrapper "Oraya v6 Edit - Bedrooms" — input socket (`userInputFlowInput`).
      Purpose: Edit both-dates-known re-entry with a known, supported guest count — continues to the Edit bedroom question.
      Where: source at canvas ≈ (2980, -160); destination at ≈ (4800, -520) (above and right of the source).
      Operation: Condition output → User Input Flow input — live-proven editor operation (2026-07-03).
- [ ] **27.** FROM `#769` Condition diamond (oraya_guest_count = "1" OR oraya_guest_count = "2" OR oraya_guest_count = "3" OR oraya_guest_count = "4" OR oraya_guest_count = "5" OR oraya_guest_count = "6" OR oraya_guest_count = "7" OR oraya_guest_count = "8") — **TRUE output** (`conditionOutputTrue`)
      TO `#670` Flow wrapper "Oraya v6 Edit - Bedrooms" — input socket (`userInputFlowInput`).
      Purpose: Edit check-out recovery with a known, supported guest count — continues to the Edit bedroom question.
      Where: source at canvas ≈ (4020, -160); destination at ≈ (4800, -520) (above and right of the source).
      Operation: Condition output → User Input Flow input — live-proven editor operation (2026-07-03).
- [ ] **28.** FROM `#770` Condition diamond (oraya_guest_count = "1" OR oraya_guest_count = "2" OR oraya_guest_count = "3" OR oraya_guest_count = "4" OR oraya_guest_count = "5" OR oraya_guest_count = "6" OR oraya_guest_count = "7" OR oraya_guest_count = "8") — **TRUE output** (`conditionOutputTrue`)
      TO `#670` Flow wrapper "Oraya v6 Edit - Bedrooms" — input socket (`userInputFlowInput`).
      Purpose: Edit guest count the guest JUST answered is supported (1–8) — continues to the Edit bedroom question.
      Where: source at canvas ≈ (4670, -400); destination at ≈ (4800, -520) (above and right of the source).
      Operation: Condition output → User Input Flow input — live-proven editor operation (2026-07-03).
- [ ] **29.** FROM `#762` Condition diamond (oraya_guest_count = "1" OR oraya_guest_count = "2" OR oraya_guest_count = "3" OR oraya_guest_count = "4" OR oraya_guest_count = "5" OR oraya_guest_count = "6" OR oraya_guest_count = "7" OR oraya_guest_count = "8") — **FALSE output** (`conditionOutputFalse`)
      TO `#736` Flow wrapper "Oraya v6 - Guests 8+ (Edit)" — input socket (`userInputFlowInput`).
      Purpose: Edit both-dates-known re-entry with more than 8 guests — Edit exact-count team review.
      Where: source at canvas ≈ (2980, -160); destination at ≈ (4541, 40) (below and right of the source).
      Operation: Condition output → User Input Flow input — live-proven editor operation (2026-07-03).
- [ ] **30.** FROM `#769` Condition diamond (oraya_guest_count = "1" OR oraya_guest_count = "2" OR oraya_guest_count = "3" OR oraya_guest_count = "4" OR oraya_guest_count = "5" OR oraya_guest_count = "6" OR oraya_guest_count = "7" OR oraya_guest_count = "8") — **FALSE output** (`conditionOutputFalse`)
      TO `#736` Flow wrapper "Oraya v6 - Guests 8+ (Edit)" — input socket (`userInputFlowInput`).
      Purpose: Edit check-out recovery with more than 8 guests — Edit exact-count team review.
      Where: source at canvas ≈ (4020, -160); destination at ≈ (4541, 40) (below and right of the source).
      Operation: Condition output → User Input Flow input — live-proven editor operation (2026-07-03).
- [ ] **31.** FROM `#770` Condition diamond (oraya_guest_count = "1" OR oraya_guest_count = "2" OR oraya_guest_count = "3" OR oraya_guest_count = "4" OR oraya_guest_count = "5" OR oraya_guest_count = "6" OR oraya_guest_count = "7" OR oraya_guest_count = "8") — **FALSE output** (`conditionOutputFalse`)
      TO `#736` Flow wrapper "Oraya v6 - Guests 8+ (Edit)" — input socket (`userInputFlowInput`).
      Purpose: Edit guest count the guest JUST answered is more than 8 — Edit exact-count team review.
      Where: source at canvas ≈ (4670, -400); destination at ≈ (4541, 40) (below and left of the source).
      Operation: Condition output → User Input Flow input — live-proven editor operation (2026-07-03).
- [ ] **32.** FROM `#763` Condition diamond (oraya_villa = "null") — **TRUE output** (`conditionOutputTrue`)
      TO `#691` Flow wrapper "Oraya v6 Edit - Villa" — input socket (`userInputFlowInput`).
      Purpose: Edit bedroom OK — 2 bedrooms fit 3–4 guests (first ask) — villa still unknown, asks the Edit villa question.
      Where: source at canvas ≈ (6100, -400); destination at ≈ (6800, -520) (above and right of the source).
      Operation: Condition output → User Input Flow input — live-proven editor operation (2026-07-03).
- [ ] **33.** FROM `#764` Condition diamond (oraya_villa = "null") — **TRUE output** (`conditionOutputTrue`)
      TO `#691` Flow wrapper "Oraya v6 Edit - Villa" — input socket (`userInputFlowInput`).
      Purpose: Edit bedroom OK — 3 bedrooms or 1–2 guests (retry ask) — villa still unknown, asks the Edit villa question.
      Where: source at canvas ≈ (5580, -40); destination at ≈ (6800, -520) (above and right of the source).
      Operation: Condition output → User Input Flow input — live-proven editor operation (2026-07-03).
- [ ] **34.** FROM `#765` Condition diamond (oraya_villa = "null") — **TRUE output** (`conditionOutputTrue`)
      TO `#691` Flow wrapper "Oraya v6 Edit - Villa" — input socket (`userInputFlowInput`).
      Purpose: Edit bedroom OK — 2 bedrooms fit 3–4 guests (retry ask) — villa still unknown, asks the Edit villa question.
      Where: source at canvas ≈ (6100, -40); destination at ≈ (6800, -520) (above and right of the source).
      Operation: Condition output → User Input Flow input — live-proven editor operation (2026-07-03).
- [ ] **35.** FROM `#674` Condition diamond (oraya_guest_count = "3" OR oraya_guest_count = "4") — **FALSE output** (`conditionOutputFalse`)
      TO `#676` Message "That bedroom setup won’t quite fit #oraya_guest_count# overnight guest…" — input socket (`textInput`).
      Purpose: Edit bedroom mismatch — 2 bedrooms with 5–8 guests (first ask) — shows the capacity explanation and re-ask.
      Where: source at canvas ≈ (5840, -520); destination at ≈ (6280, -520) (level with and right of the source).
      Operation: Condition output → Text input — live-proven editor operation (2026-07-03).
- [ ] **36.** FROM `#763` Condition diamond (oraya_villa = "null") — **FALSE output** (`conditionOutputFalse`)
      TO `#694` Flow wrapper "Oraya v6 Edit - Confirm" — input socket (`userInputFlowInput`).
      Purpose: Edit bedroom OK (first ask), villa already known — continues to the updated confirmation summary.
      Where: source at canvas ≈ (6100, -400); destination at ≈ (7320, -400) (level with and right of the source).
      Operation: Condition output → User Input Flow input — live-proven editor operation (2026-07-03).
- [ ] **37.** FROM `#764` Condition diamond (oraya_villa = "null") — **FALSE output** (`conditionOutputFalse`)
      TO `#694` Flow wrapper "Oraya v6 Edit - Confirm" — input socket (`userInputFlowInput`).
      Purpose: Edit bedroom OK (retry, 3 bedrooms or 1–2 guests), villa already known — continues to the updated confirmation summary.
      Where: source at canvas ≈ (5580, -40); destination at ≈ (7320, -400) (above and right of the source).
      Operation: Condition output → User Input Flow input — live-proven editor operation (2026-07-03).
- [ ] **38.** FROM `#765` Condition diamond (oraya_villa = "null") — **FALSE output** (`conditionOutputFalse`)
      TO `#694` Flow wrapper "Oraya v6 Edit - Confirm" — input socket (`userInputFlowInput`).
      Purpose: Edit bedroom OK (retry, 2 bedrooms fit 3–4), villa already known — continues to the updated confirmation summary.
      Where: source at canvas ≈ (6100, -40); destination at ≈ (7320, -400) (above and right of the source).
      Operation: Condition output → User Input Flow input — live-proven editor operation (2026-07-03).
- [ ] **39.** FROM `#693` Message "Lovely choice 😊" — **output** (`textOutput`)
      TO `#694` Flow wrapper "Oraya v6 Edit - Confirm" — input socket (`userInputFlowInput`).
      Purpose: Edit villa the guest JUST chose continues to the updated confirmation summary.
      Where: source at canvas ≈ (7190, -520); destination at ≈ (7320, -400) (below and right of the source).
      Operation: Text output → User Input Flow input — live-proven editor operation (2026-07-03).

## Hub-by-hub visual map

One block per remaining merge point. The first line is the connection the import keeps automatically; every `draw #N` line is an item from the list above.

### Hub `#600` — guest-count question — merges the guest-unknown exits of the original and cloned "guest known?" checks ≈ (5200, -1900)
```
  #440 TRUE output  ──────────────►  #600   (kept automatically by the import)
  #750 TRUE output  ── draw #1 ──►  #600
  #752 TRUE output  ── draw #2 ──►  #600
  #754 TRUE output  ── draw #3 ──►  #600
  #756 TRUE output  ── draw #4 ──►  #600
```

### Hub `#610` — bedroom question — merges every supported-guest-count OK exit ≈ (5200, -1600)
```
  #602 TRUE output  ──────────────►  #610   (kept automatically by the import)
  #751 TRUE output  ── draw #5 ──►  #610
  #753 TRUE output  ── draw #6 ──►  #610
  #755 TRUE output  ── draw #7 ──►  #610
  #757 TRUE output  ── draw #8 ──►  #610
  #766 TRUE output  ── draw #9 ──►  #610
```

### Hub `#466` — large-group exact-count team review — merges every More-than-8 exit ≈ (5451, -2777)
```
  #602 FALSE output  ──────────────►  #466   (kept automatically by the import)
  #751 FALSE output  ── draw #10 ──►  #466
  #753 FALSE output  ── draw #11 ──►  #466
  #755 FALSE output  ── draw #12 ──►  #466
  #757 FALSE output  ── draw #13 ──►  #466
  #766 FALSE output  ── draw #14 ──►  #466
```

### Hub `#480` — villa question — merges every capacity-OK, villa-unknown exit ≈ (6641, -1546)
```
  #470 TRUE output  ──────────────►  #480   (kept automatically by the import)
  #758 TRUE output  ── draw #15 ──►  #480
  #759 TRUE output  ── draw #16 ──►  #480
  #760 TRUE output  ── draw #17 ──►  #480
```

### Hub `#616` — bedroom capacity explanation + re-ask ≈ (6760, -1600)
```
  #613 TRUE output  ──────────────►  #616   (kept automatically by the import)
  #614 FALSE output  ── draw #18 ──►  #616
```

### Hub `#490` — confirmation summary entry — merges the villa-known exits and the villa-just-chosen path ≈ (6820, -933)
```
  #470 FALSE output  ──────────────►  #490   (kept automatically by the import)
  #758 FALSE output  ── draw #19 ──►  #490
  #759 FALSE output  ── draw #20 ──►  #490
  #760 FALSE output  ── draw #21 ──►  #490
  #604 output  ── draw #22 ──►  #490
```

### Hub `#655` — Edit date-trouble escalation message — merges both refine-failure exits ≈ (3500, -520)
```
  #654 TRUE output  ──────────────►  #655   (kept automatically by the import)
  #767 TRUE output  ── draw #23 ──►  #655
```

### Hub `#661` — Edit guest-count question — merges every Edit guest-unknown exit ≈ (4020, -520)
```
  #660 TRUE output  ──────────────►  #661   (kept automatically by the import)
  #761 TRUE output  ── draw #24 ──►  #661
  #768 TRUE output  ── draw #25 ──►  #661
```

### Hub `#670` — Edit bedroom question — merges every Edit supported-guest-count OK exit ≈ (4800, -520)
```
  #663 TRUE output  ──────────────►  #670   (kept automatically by the import)
  #762 TRUE output  ── draw #26 ──►  #670
  #769 TRUE output  ── draw #27 ──►  #670
  #770 TRUE output  ── draw #28 ──►  #670
```

### Hub `#736` — Edit large-group exact-count team review — merges every Edit More-than-8 exit ≈ (4541, 40)
```
  #663 FALSE output  ──────────────►  #736   (kept automatically by the import)
  #762 FALSE output  ── draw #29 ──►  #736
  #769 FALSE output  ── draw #30 ──►  #736
  #770 FALSE output  ── draw #31 ──►  #736
```

### Hub `#691` — Edit villa question — merges every Edit capacity-OK, villa-unknown exit ≈ (6800, -520)
```
  #690 TRUE output  ──────────────►  #691   (kept automatically by the import)
  #763 TRUE output  ── draw #32 ──►  #691
  #764 TRUE output  ── draw #33 ──►  #691
  #765 TRUE output  ── draw #34 ──►  #691
```

### Hub `#676` — Edit bedroom capacity explanation + re-ask ≈ (6280, -520)
```
  #673 TRUE output  ──────────────►  #676   (kept automatically by the import)
  #674 FALSE output  ── draw #35 ──►  #676
```

### Hub `#694` — Edit confirmation summary entry ≈ (7320, -400)
```
  #690 FALSE output  ──────────────►  #694   (kept automatically by the import)
  #763 FALSE output  ── draw #36 ──►  #694
  #764 FALSE output  ── draw #37 ──►  #694
  #765 FALSE output  ── draw #38 ──►  #694
  #693 output  ── draw #39 ──►  #694
```

## Verify after drawing all 39

Save → close the editor completely → reopen → export, then run BOTH (each must succeed):

```
node scripts/compare-whatchimp-roundtrip.mjs Oraya_natural_intake_v6.txt <re-export>   # must print PRESERVED, exit 0
node scripts/validate-whatchimp-flow.mjs <re-export> --strict-binding                  # must report 0 errors, exit 0
```

If the comparator reports ANY lost edge or unexpected terminal, or the validator reports a `single-parent-contract` hub mismatch, a drawn connection is missing or wrong — fix it in the editor and re-export before any live testing (checklist section C).
