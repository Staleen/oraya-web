# Oraya Natural Stay Intake v6 — post-import verification checklist (interactive candidate)

**Machine-generated by `scripts/generate-whatchimp-v6.mjs` — do not edit by hand; it is regenerated with the artifact.**

**Applies to exactly:** `Oraya_natural_intake_v6.txt` with SHA-256 `4383A12F4EEE2E1E721F94BB03DA584B0383D3F47A69BC77C48F659C2E841659`. If your file's hash differs, regenerate this checklist.

**There are ZERO connections to draw.** This candidate serializes convergence ONLY where every parent is an Inline Button / list Row AND the target is a Text (field-binding schema evidence: `roundtrips/Oraya_natural_intake_v6.button-evidence.saved-reexport.txt`). Round trip #4 (2026-07-04, fixture `roundtrips/Oraya_natural_intake_v6.roundtrip-4.saved-reexport.txt`) proved a fresh import PRESERVES serialized control → Text convergence at 30-way, 18-way, and 2-way fan-in but silently DROPS direct Rows → User Input Flow connections — which is why every "More than 6" row now routes through the shared acknowledgement Text before the large-group wrapper. **Inspect the five "More than 6" connectors explicitly** — they are the repaired class; if anything arrives loose, STOP and report; do not draw around it.

## 1. Import verification (fresh disposable bot)

- [ ] Import `Oraya_natural_intake_v6.txt` into a **fresh disposable test bot** — the import and the first open must show **no warning** (any "infinite loop" or similar message = stop and report).
- [ ] Visually confirm **no loose connectors**: every Interactive's buttons/rows carry a forward line, the shared acknowledgement Texts receive all their inbound lines, and no node dangles.

## 2. Click-through — every control writes its exact value and advances exactly once

For each control below: tap it in a test conversation, then open the subscriber's custom fields and confirm the stated field holds the EXACT stated value (the stored value must equal the visible label — the export schema carries no separate value field, so this is the platform behavior being verified). Each press must advance the conversation exactly once (no duplicate next message, no restart of the intake flow).

### Interactive `#800` — "How many guests will be staying overnight?"
- [ ] `#803` Rows **"1"** → writes `oraya_guest_count` (`custom_57693`) = `1`; advances once into #860 Text.
- [ ] `#804` Rows **"2"** → writes `oraya_guest_count` (`custom_57693`) = `2`; advances once into #860 Text.
- [ ] `#805` Rows **"3"** → writes `oraya_guest_count` (`custom_57693`) = `3`; advances once into #860 Text.
- [ ] `#806` Rows **"4"** → writes `oraya_guest_count` (`custom_57693`) = `4`; advances once into #860 Text.
- [ ] `#807` Rows **"5"** → writes `oraya_guest_count` (`custom_57693`) = `5`; advances once into #860 Text.
- [ ] `#808` Rows **"6"** → writes `oraya_guest_count` (`custom_57693`) = `6`; advances once into #860 Text.
- [ ] `#809` Rows **"More than 6"** → writes `oraya_guest_count` (`custom_57693`) = `More than 6`; advances once into #865 Text.

### Interactive `#810` — "How many guests will be staying overnight?"
- [ ] `#813` Rows **"1"** → writes `oraya_guest_count` (`custom_57693`) = `1`; advances once into #860 Text.
- [ ] `#814` Rows **"2"** → writes `oraya_guest_count` (`custom_57693`) = `2`; advances once into #860 Text.
- [ ] `#815` Rows **"3"** → writes `oraya_guest_count` (`custom_57693`) = `3`; advances once into #860 Text.
- [ ] `#816` Rows **"4"** → writes `oraya_guest_count` (`custom_57693`) = `4`; advances once into #860 Text.
- [ ] `#817` Rows **"5"** → writes `oraya_guest_count` (`custom_57693`) = `5`; advances once into #860 Text.
- [ ] `#818` Rows **"6"** → writes `oraya_guest_count` (`custom_57693`) = `6`; advances once into #860 Text.
- [ ] `#819` Rows **"More than 6"** → writes `oraya_guest_count` (`custom_57693`) = `More than 6`; advances once into #865 Text.

### Interactive `#870` — "How many bedrooms would you like?"
- [ ] `#871` Inline Button **"1 bedroom"** → writes `oraya_bedroom_count` (`custom_69114`) = `1 bedroom`; advances once into #930 Text.
- [ ] `#872` Inline Button **"2 bedrooms"** → writes `oraya_bedroom_count` (`custom_69114`) = `2 bedrooms`; advances once into #930 Text.
- [ ] `#873` Inline Button **"3 bedrooms"** → writes `oraya_bedroom_count` (`custom_69114`) = `3 bedrooms`; advances once into #930 Text.

### Interactive `#875` — "How many bedrooms would you like?"
- [ ] `#876` Inline Button **"1 bedroom"** → writes `oraya_bedroom_count` (`custom_69114`) = `1 bedroom`; advances once into #930 Text.
- [ ] `#877` Inline Button **"2 bedrooms"** → writes `oraya_bedroom_count` (`custom_69114`) = `2 bedrooms`; advances once into #930 Text.
- [ ] `#878` Inline Button **"3 bedrooms"** → writes `oraya_bedroom_count` (`custom_69114`) = `3 bedrooms`; advances once into #930 Text.

### Interactive `#880` — "How many bedrooms would you like?"
- [ ] `#881` Inline Button **"1 bedroom"** → writes `oraya_bedroom_count` (`custom_69114`) = `1 bedroom`; advances once into #930 Text.
- [ ] `#882` Inline Button **"2 bedrooms"** → writes `oraya_bedroom_count` (`custom_69114`) = `2 bedrooms`; advances once into #930 Text.
- [ ] `#883` Inline Button **"3 bedrooms"** → writes `oraya_bedroom_count` (`custom_69114`) = `3 bedrooms`; advances once into #930 Text.

### Interactive `#935` — "Which villa would you prefer?"
- [ ] `#936` Inline Button **"Villa Mechmech"** → writes `oraya_villa` (`custom_57698`) = `Villa Mechmech`; advances once into #938 Text.
- [ ] `#937` Inline Button **"Villa Byblos"** → writes `oraya_villa` (`custom_57698`) = `Villa Byblos`; advances once into #938 Text.

### Interactive `#1002` — "Which villa would you prefer?"
- [ ] `#1003` Inline Button **"Villa Mechmech"** → writes `oraya_villa` (`custom_57698`) = `Villa Mechmech`; advances once into #938 Text.
- [ ] `#1004` Inline Button **"Villa Byblos"** → writes `oraya_villa` (`custom_57698`) = `Villa Byblos`; advances once into #938 Text.

### Interactive `#1012` — "Which villa would you prefer?"
- [ ] `#1013` Inline Button **"Villa Mechmech"** → writes `oraya_villa` (`custom_57698`) = `Villa Mechmech`; advances once into #938 Text.
- [ ] `#1014` Inline Button **"Villa Byblos"** → writes `oraya_villa` (`custom_57698`) = `Villa Byblos`; advances once into #938 Text.

### Interactive `#1022` — "Which villa would you prefer?"
- [ ] `#1023` Inline Button **"Villa Mechmech"** → writes `oraya_villa` (`custom_57698`) = `Villa Mechmech`; advances once into #938 Text.
- [ ] `#1024` Inline Button **"Villa Byblos"** → writes `oraya_villa` (`custom_57698`) = `Villa Byblos`; advances once into #938 Text.

### Interactive `#1130` — "Lovely — I have your dates as #oraya_check_in# → #oraya_check_out# ✅"
- [ ] `#1131` Inline Button **"Continue"** → writes `oraya_dates_confirmed_text` (`custom_58532`) = `Continue`; advances once into #1150 Text.

### Interactive `#1134` — "Lovely — I have your dates as #oraya_check_in# → #oraya_check_out# ✅"
- [ ] `#1135` Inline Button **"Continue"** → writes `oraya_dates_confirmed_text` (`custom_58532`) = `Continue`; advances once into #1150 Text.

### Interactive `#1136` — "No problem at all 😊 You can pick your exact dates on our secure booki"
- [ ] `#1137` Inline Button **"Continue"** → writes `oraya_dates_confirmed_text` (`custom_58532`) = `Continue`; advances once into #1200 Text.

### Interactive `#1138` — "No problem at all 😊 You can pick your exact dates on our secure booki"
- [ ] `#1139` Inline Button **"Continue"** → writes `oraya_dates_confirmed_text` (`custom_58532`) = `Continue`; advances once into #1200 Text.

### Interactive `#1140` — "Lovely — I have your dates as #oraya_check_in# → #oraya_check_out# ✅"
- [ ] `#1141` Inline Button **"Continue"** → writes `oraya_dates_confirmed_text` (`custom_58532`) = `Continue`; advances once into #1150 Text.

### Interactive `#1142` — "Lovely — I have your dates as #oraya_check_in# → #oraya_check_out# ✅"
- [ ] `#1143` Inline Button **"Continue"** → writes `oraya_dates_confirmed_text` (`custom_58532`) = `Continue`; advances once into #1150 Text.

### Interactive `#1144` — "No problem at all 😊 You can pick your exact dates on our secure booki"
- [ ] `#1145` Inline Button **"Continue"** → writes `oraya_dates_confirmed_text` (`custom_58532`) = `Continue`; advances once into #1200 Text.

### Interactive `#1160` — "How many guests will be staying overnight?"
- [ ] `#1163` Rows **"1"** → writes `oraya_guest_count` (`custom_57693`) = `1`; advances once into #860 Text.
- [ ] `#1164` Rows **"2"** → writes `oraya_guest_count` (`custom_57693`) = `2`; advances once into #860 Text.
- [ ] `#1165` Rows **"3"** → writes `oraya_guest_count` (`custom_57693`) = `3`; advances once into #860 Text.
- [ ] `#1166` Rows **"4"** → writes `oraya_guest_count` (`custom_57693`) = `4`; advances once into #860 Text.
- [ ] `#1167` Rows **"5"** → writes `oraya_guest_count` (`custom_57693`) = `5`; advances once into #860 Text.
- [ ] `#1168` Rows **"6"** → writes `oraya_guest_count` (`custom_57693`) = `6`; advances once into #860 Text.
- [ ] `#1169` Rows **"More than 6"** → writes `oraya_guest_count` (`custom_57693`) = `More than 6`; advances once into #865 Text.

### Interactive `#1170` — "How many bedrooms would you like?"
- [ ] `#1171` Inline Button **"1 bedroom"** → writes `oraya_bedroom_count` (`custom_69114`) = `1 bedroom`; advances once into #930 Text.
- [ ] `#1172` Inline Button **"2 bedrooms"** → writes `oraya_bedroom_count` (`custom_69114`) = `2 bedrooms`; advances once into #930 Text.
- [ ] `#1173` Inline Button **"3 bedrooms"** → writes `oraya_bedroom_count` (`custom_69114`) = `3 bedrooms`; advances once into #930 Text.

### Interactive `#1182` — "Which villa would you prefer?"
- [ ] `#1183` Inline Button **"Villa Mechmech"** → writes `oraya_villa` (`custom_57698`) = `Villa Mechmech`; advances once into #938 Text.
- [ ] `#1184` Inline Button **"Villa Byblos"** → writes `oraya_villa` (`custom_57698`) = `Villa Byblos`; advances once into #938 Text.

### Interactive `#1210` — "How many guests will be staying overnight?"
- [ ] `#1213` Rows **"1"** → writes `oraya_guest_count` (`custom_57693`) = `1`; advances once into #860 Text.
- [ ] `#1214` Rows **"2"** → writes `oraya_guest_count` (`custom_57693`) = `2`; advances once into #860 Text.
- [ ] `#1215` Rows **"3"** → writes `oraya_guest_count` (`custom_57693`) = `3`; advances once into #860 Text.
- [ ] `#1216` Rows **"4"** → writes `oraya_guest_count` (`custom_57693`) = `4`; advances once into #860 Text.
- [ ] `#1217` Rows **"5"** → writes `oraya_guest_count` (`custom_57693`) = `5`; advances once into #860 Text.
- [ ] `#1218` Rows **"6"** → writes `oraya_guest_count` (`custom_57693`) = `6`; advances once into #860 Text.
- [ ] `#1219` Rows **"More than 6"** → writes `oraya_guest_count` (`custom_57693`) = `More than 6`; advances once into #865 Text.

### Interactive `#1220` — "How many bedrooms would you like?"
- [ ] `#1221` Inline Button **"1 bedroom"** → writes `oraya_bedroom_count` (`custom_69114`) = `1 bedroom`; advances once into #930 Text.
- [ ] `#1222` Inline Button **"2 bedrooms"** → writes `oraya_bedroom_count` (`custom_69114`) = `2 bedrooms`; advances once into #930 Text.
- [ ] `#1223` Inline Button **"3 bedrooms"** → writes `oraya_bedroom_count` (`custom_69114`) = `3 bedrooms`; advances once into #930 Text.

### Interactive `#1232` — "Which villa would you prefer?"
- [ ] `#1233` Inline Button **"Villa Mechmech"** → writes `oraya_villa` (`custom_57698`) = `Villa Mechmech`; advances once into #938 Text.
- [ ] `#1234` Inline Button **"Villa Byblos"** → writes `oraya_villa` (`custom_57698`) = `Villa Byblos`; advances once into #938 Text.

## 3. Persistence + export verification

- [ ] Save → close the editor completely → reopen: every connection from steps 1–2 must still be present; re-click at least one control per Interactive.
- [ ] Export the flow and verify mechanically (both must succeed):

```
node scripts/compare-whatchimp-roundtrip.mjs Oraya_natural_intake_v6.txt <re-export>   # must print PRESERVED, exit 0
node scripts/validate-whatchimp-flow.mjs <re-export> --strict-binding                  # must report 0 errors, exit 0
```

- [ ] Run one complete conversation to the final summary and confirm the saved lead (dates, guests, bedrooms, villa) in `/admin/leads` + Supabase `whatsapp_leads.raw_payload`.

If ANY control is refused, mis-writes its field, double-advances, restarts the flow, or loses its line after save/reopen — **stop and report immediately**; that is new platform evidence. Do not insert nodes or draw around it. Production stays untouched.
