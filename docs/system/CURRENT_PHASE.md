# Current Phase - Phase 16A closeout (COMPLETE) / Phase 16B complete payment system

**Active card-production mission (2026-08-09):** NetCommerce onboarding and live merchant activation for Visa/Mastercard Unified Checkout are verified complete from the owner's activation and Business Center emails. Do not reopen onboarding as a blocker. The remaining card work is tracked in [PHASE_16B_CARD_PRODUCTION_ACTIVATION_MISSION.md](PHASE_16B_CARD_PRODUCTION_ACTIVATION_MISSION.md): secure production key configuration, webhook subscription/proof, non-charging readiness, a controlled real-card transaction, refund/reconciliation/settlement evidence, and deliberate monitored activation. Real charging remains off until those evidence gates pass.

**Authoritative Phase 16B update (2026-08-09; supersedes older temporary-webhook/missing-JWT wording below):** PR #113 merged the canonical card bridge (`20124c6`): booking-time and standalone card collections now use `payment_requests`, request-scoped attempts, the immutable transaction ledger, and the same idempotent browser/webhook reconciliation path. PR #114 (`9506f5f`) merged and production-deployed the provider-gate/operations closeout: a visible provider checklist, ambiguous-attempt/failed-event queue, merchant references, gross/fee/net summaries, and a separate exact Apple Pay activation flag. The Apple-only provider-ledger migration is installed and passed a rolled-back live classification/idempotency proof; desktop, true 390px mobile, unauthenticated-boundary, and fail-closed deployed checks passed. The application is ready to accept provider configuration but remains deliberately dark: Vercel currently has none of the required `PAYMENT_PROVIDER` or `NETCOMMERCE_CYBERSOURCE_*` variables, live Supabase has no `payments_live_enabled` row, Apple Pay is not enrolled/effective-host-verified/device-tested, and no native Whish/OMT/Suyool merchant API contract or TMS saved-card approval has been supplied. The current production redirect means an Apple enrollment must verify the effective checkout host `www.stayoraya.com` while that routing remains in place, not infer it from the bare guest-facing URL. Those are external activation gates, not missing ordinary application code.

**Phase 16D Stage A shipped DARK (2026-08-10; revised same day per PR #120 review):** the hybrid preloaded door-PIN magazine (Aqara A100 front door + U100 gate per property) now has its server-only foundation and nothing more. Committed but **unapplied** human-run migration [sql/phase-16d-access-magazine.sql](../../sql/phase-16d-access-magazine.sql): `access_credentials` (property-isolated magazine rows; opaque slot/batch labels; normal vs emergency class; AES-256-GCM ciphertext erased at destruction while the versioned keyed fingerprint + its key id are retained forever; per-lock A100/U100 install and deletion confirmations as write-once **timestamp + actor pairs** — `available` requires BOTH complete pairs and `destroyed` requires complete deletion pairs for every installed lock; comprehensive state/fact-consistency CHECKs binding disclosure, quarantine, deletion, and destruction facts to their proper states; set-once booking linkage with primary/backup/replacement lineage **verified in-database against the predecessor row** (same property + booking, consumed/quarantined predecessor, valid relationship, no self-reference); DB-trigger-enforced lifecycle `installing → available → disclosed → quarantined → deletion_pending → destroyed` where disclosed never returns to available) plus append-only `access_credential_events` written **transactionally by an AFTER trigger for every mutation** — direct service-role updates cannot bypass the audit ledger — with allow-listed reason codes only (no free-text surface that could persist a PIN). RLS with no client policies, service-role-only grants, no deletes. Server-only code: [lib/access/pin-vault.ts](../../lib/access/pin-vault.ts) (dedicated keyring env `ACCESS_PIN_VAULT_KEYS` + versioned fingerprint keyring `ACCESS_PIN_FINGERPRINT_KEYS` — never `ADMIN_SECRET`; **both unset in every environment**, fail-closed, runtime browser-context guard; duplicate detection survives fingerprint-key rotation via all-keys candidate fingerprinting; emergency-compromise runbook in ENVIRONMENT_MAP), [lib/access/pin-generator.ts](../../lib/access/pin-generator.ts) (CSPRNG six-digit memorable generator, broad rejection families, bounded attempts, leading zero rejected for the manual dual-lock copy workflow; a deterministic full-enumeration test pins the accepted population at ~336k codes across ≥15 feature shapes with no dominant template), [lib/access/pin-lifecycle.ts](../../lib/access/pin-lifecycle.ts). Focused tests 57/57. **No real PIN exists; no ops UI, API route, booking allocation, guest reveal, WhatsApp delivery, Aqara call, or payment/confirmation change shipped.** The Arrival Guide stays confirmed-only and PIN-free; payment still never auto-confirms a booking. Next stages each need separate approval: **B** — `/ops` magazine console with one-PIN loading sessions, separate A100/U100 confirmations, counts-only widget, quarantine/deletion workflow, and a **3-PIN physical pilot before bulk loading** (also the point where David sets the two new Vercel secrets and runs the migration); **C** — atomic booking allocation, one backup, stop/escalate, operator-mediated delivery, checkout deletion queue (**blocked on David's explicit access-eligibility + role-policy decision**); **D** — optional confirmed-only Arrival Guide extra-tap reveal on the existing signed view-token boundary; **E** — optional read-only Aqara device/event monitoring (no writes, no remote unlock, cannot manage PINs).

**Updated:** 2026-08-09 (Phase 16B complete payment-system mission)
**Status:** **Phase 16A is COMPLETE for its approved production scope (WhatChimp production wiring locked 2026-07-09).** The approved scope is delivered and live: natural stay intake + secure website prefill handoff (Book a Stay), the direct event redirect to `/events/inquiry` (Plan an Event — no duplicate WhatsApp event intake), and the identify-first Guest Identification v2 booking-support flow — plus the manual WhatChimp closeout completed by David, and the production-readiness + guest-usage documentation. The v6 structured-date natural intake is locked/operator-approved (PR #69 merged `92c1441`, production-deployed; DECISIONS_LOG 2026-07-09; KNOWN_BUGS #10 RESOLVED). The knowledge is preserved as documentation (see [BUTLER_PLAYBOOK.md](BUTLER_PLAYBOOK.md) "Phase 16A WhatChimp production builder (LOCKED 2026-07-09)", [PHASE_16A_PRODUCTION_READINESS.md](PHASE_16A_PRODUCTION_READINESS.md), [WHATSAPP_GUEST_USAGE_GUIDE.md](WHATSAPP_GUEST_USAGE_GUIDE.md), and [DECISIONS_LOG.md](DECISIONS_LOG.md) 2026-07-09), **not** as committed WhatChimp exports. Future WhatsApp enhancements (WhatsApp-side booking submission, payment-status replies, location/PIN/access, new channels) are **new scoped work, not remaining Phase 16A closeout**. **Phase 16B and Phase 16C remain separate, still-open workstreams** — this Phase 16A closeout does not close them. Phase 16B is active: PR #64 merged the NetCommerce / CyberSource Unified Checkout sandbox foundation after successful NetCommerce testing and saved-card removal. The temporary Preview QA bypass/auto-confirm behavior has been retired from production-bound code. Normal successful payment updates payment fields but leaves `bookings.status` pending for Oraya operations. The guest-facing Reserve payment journey is polished while live production charging remains blocked: unavailable checkout creates the booking request and shows a payment-pending handoff instead of technical readiness/setup-failure text; admin-only surfaces still show the real readiness state. **Update 2026-07-24/25 (merged PRs #85–#94):** the health-check remediation waves, Plan 3, and Plan 4 landed — admin password hardening + email recovery, CI + `npm test`, the Next 16 / React 19 upgrade, durable payment-attempt idempotency (`payment_attempts`), `GET /api/health`, a fail-closed reconciliation core behind a temporary raw-body HMAC webhook receiver, manual-first refunds ([REFUND_RUNBOOK.md](REFUND_RUNBOOK.md)), and a fail-closed `payments_live_enabled` switch. **Update 2026-08-06 (PR #104):** payment outcomes and attempt transitions are retry-safe and monotonic; PR #104 completes that state-safety work only. Production checkout remains blocked on missing production security and provider setup: JWT request authentication, response MLE, webhook JWE decryption, timestamped digital-signature verification, replay protection, webhook subscription/setup, production credentials/env, human-run SQL migrations, and declined-card validation. The current placeholder webhook configuration must not be enabled or subscribed in production. Settlement reconciliation, payment email validation, WhatsApp payment-status replies, and Instant Book execution also remain outstanding; refunds automation is deliberately deferred by policy.

**Current correction to the historical summary above:** JWT/JWS, Payments request/response MLE, webhook JWE decryption, timestamped signature verification, durable replay claims, and the required additive migrations are implemented; PR #112 is merged. The remaining production gates are provider-issued/configured credentials, subscription/URL approval, delivered sandbox webhook and declined-card evidence, reconciliation proof, and explicit activation. The production switch is off.

This file is rewritten at every phase transition. Treat it as a snapshot, not a log.

**Phase 16B-2B1 update (supersedes older missing-security wording below):** CyberSource requests use short-lived JWT/JWS v2 authentication. `/pts/v2/payments` uses request and response MLE; `/uc/v1/sessions` uses JWT without request MLE. PR #112 added the matching webhook JWE/signature/replay contract. Any unverified post-submission outcome remains ambiguous and retry-blocking. Production remains disabled pending provider credentials/configuration, subscription approval, delivered sandbox evidence, decline/reconciliation proof, and deliberate activation.

**Phase 16B scope correction (owner decision 2026-08-09):** Phase 16B is Oraya's complete business payment system. NetCommerce/CyberSource is one active provider workstream, not the phase boundary. The phase includes cash and manual receipts, booking-linked and standalone Oraya payment requests, credit/debit cards, Apple Pay, Whish, OMT Pay, Western Union, the provisionally identified Suyool/"Sunbook Pay" rail, partial/multiple payments, refunds/reversals/reconciliation, and tokenized saved cards for members as a later consent- and provider-gated workstream. This supersedes older text that permanently places saved cards outside Phase 16B or models a booking row as the only payment-link object. The current NetCommerce launch still omits saved cards, and no newly listed provider is live merely because it is in scope. The canonical mission and delivery order are in [PHASE_16B_PAYMENT_SYSTEM_MISSION.md](PHASE_16B_PAYMENT_SYSTEM_MISSION.md); implementation begins with the shared request/transaction ledger and cash workflow.

**Phase 16B-F1/F2/F3 implementation update (2026-08-09):** the additive canonical `payment_requests`, `payment_transactions`, and `payment_provider_events` schema is installed on the live ORAYA Supabase project with RLS/default-deny client access. Atomic service-role RPCs now record and reverse manual receipts while projecting booking/request balances; transaction facts are immutable and corrections append linked reversal rows. The implementation branch adds the `/ops/payments` collection workspace, cash/bank/Whish/OMT/Western Union/Suyool manual receipt flows, booking-linked or standalone secure links, `/pay/[token]`, cancellation, partial receipts, and exact projection rollback. The schema passed rolled-back live cash, partial-payment, and reversal smoke tests with zero retained test rows. This foundation is not yet a native Whish/OMT/Suyool integration and does not activate cards or Apple Pay.

**Phase 16B-NC webhook-security update (2026-08-09):** PR #112 is merged. CyberSource payment/Unified Checkout webhooks now use compact-JWE payload decryption, distinct digital-signature credentials, `v-c-signature` verification over `timestamp + "." + decryptedPayload`, a five-minute tolerance, and replay-safe durable provider-event claiming before reconciliation. The additive `payment_attempts.payment_request_id` link is installed on live Supabase and its replay constraint passed a rolled-back smoke test. This code does not activate production checkout; real provider-issued keys, subscription/URL approval, delivered sandbox webhook evidence, decline/reconciliation evidence, and the explicit live switch remain external gates.

**Phase 16B card-request bridge update (2026-08-09):** the active clean branch routes both booking-time card collection and operator-created standalone links through canonical `payment_requests`. `/pay/[token]` exposes the NetCommerce card action only when the server readiness gate is truly open; the shared Unified Checkout page supports booking or request tokens; a request-scoped attempt is claimed before authorization; and the provider-confirmed receipt is atomically appended to `payment_transactions` while updating request and booking projections. The live additive schema/RPC passed rolled-back provider-payment, idempotency, and manual-vs-provider race smoke tests with zero retained rows. Code and schema readiness are not provider activation.

---

## Active phase

**Phase 16A closeout (COMPLETE for approved scope) + Phase 16B complete payment system (active; NetCommerce is one workstream).**

Phase 16A's approved production scope is complete (WhatChimp production wiring locked 2026-07-09 — natural stay intake, secure website handoff, event redirect, Guest Identification v2; see [BUTLER_PLAYBOOK.md](BUTLER_PLAYBOOK.md) "Phase 16A WhatChimp production builder (LOCKED 2026-07-09)"). The shipped foundation is:

- `/api/butler/health|event-types|addons|availability|normalize-dates|lead|prefill|identify|confirmed-guest-info|booking-lookup` are live and guarded by `BUTLER_WEBHOOK_SECRET` (or `BUTLER_PREFILL_SECRET` for the public prefill route).
- `whatsapp_leads` is the operational source of truth for WhatsApp-originated booking intent.
- `/book` accepts a short-lived opaque handoff token (`?h=...`) and continues the conversation on the website without retyping.
- A successful booking best-effort links `whatsapp_leads.linked_booking_id` to the new booking row via `butler_prefill_token` in the booking POST body. This closes the lead -> booking provenance loop without changing locked-pipeline behavior on failure.
- WhatsApp identity v2 ships through `/api/butler/identify`: subscriber-id is the primary continuity key; `chat_id` is diagnostic-only and is **never** treated as a phone number; the reference fallback gates disclosure behind an identity proof (email OR full name, normalized exact-after-normalization match); the response is enriched with the 8-character public booking reference plus the signed `/booking/view/[token]` URL on identity-established active branches.
- `/api/butler/confirmed-guest-info` is the dedicated confirmed-guest boundary: allows the reference, villa, dates, signed booking-view URL, operator-configured `butler_checkin_guidance`, and an explicit `location_access_note`; blocks PIN, exact GPS, payment links, admin notes, internal IDs, and confirm/cancel tokens.
- The booking-reference + change/cancel website CTAs pre-fill WhatsApp with **plain human sentences** (`"Check my booking <ref>"` / `"Help with my booking <ref>"`) - the earlier `#ORAYA_REF:` / `#ORAYA_CHANGE:` marker scheme has been withdrawn. See [DECISIONS_LOG.md](DECISIONS_LOG.md) "WhatsApp CTA prefills reverted to plain human sentences" (2026-06-03).
- `/api/butler/identify` accepts an optional `message_text` field that runs a **word-boundary-anchored 8-character hex** extractor ([lib/butler/extract-booking-reference.ts](../../lib/butler/extract-booking-reference.ts)) so non-WhatChimp channels (Telegram, Messenger, WhatsApp Cloud API) that DO expose inbound text auto-skip the reference ask. The WhatChimp tenant does not expose inbound text today, so the production path still asks for the reference once.

The following are **new scoped work, not remaining Phase 16A closeout** (the approved Phase 16A production scope is complete as of 2026-07-09). They are captured here so their boundaries and policy guardrails are not lost; picking any of them up requires a fresh scoped task:

1. **WhatChimp confirmed-guest flow wiring.** The `/api/butler/confirmed-guest-info` endpoint exists, but a WhatChimp flow that calls it on identity-established confirmed guests is a future enhancement (the shipped Guest Identification v2 covers booking-support status via `/api/butler/identify`).
2. **Confirmed-guest routing refinement.** Once wired, ensure the bot routes correctly between "deliver confirmed-guest info" (active, identity established) vs "wait for confirmation" (pending) vs "acknowledge cancellation" (cancelled) without leaking sensitive fields.
3. **Generic AI containment.** Ensure the generic AI Training layer does not answer booking-sensitive confirmed-guest questions (villa, dates, view URL, check-in guidance, location-access policy) outside the `confirmed-guest-info` boundary. The orchestrator is the single source of truth per turn.
4. **`POST /api/butler/flow-submit`** is still the remaining WhatsApp write-capable booking-adapter scope IF/when WhatsApp-side booking submission is scoped in. The current architecture keeps `/book` as the authoritative booking surface and WhatsApp as lead-capture + secure handoff.
5. **Location / PIN / access automation is explicitly NOT 16A** - it remains Phase 16D. The Butler must never quote a PIN, gate code, or exact arrival coordinates today.

The currently shipped Phase 16B NetCommerce workstream is active on the website Reserve path:

- payment-link columns exist on `bookings` (the additive `phase-16b1` migration shipped, and the `phase-16b4` Credit Libanais provider-compat constraint widening migration is staged).
- admin + guest runtime surfaces understand `payment_link_*`.
- `/book` is now a **three-step** flow (Villa & Dates -> Stay Setup -> Review & Guest Details). The hidden Step 4 / standalone Guest Details step / four-step journey were all explicitly rejected in favor of this consolidation.
- Step 3 presents two explicit Reserve actions: **primary "Continue to secure payment"** (gold solid CTA; eligible stays open hosted checkout after booking creation, while unavailable/ineligible checkout preserves the request and routes to the pending-payment handoff) and **secondary "Reserve now, pay later"** (gold outline / transparent background CTA; submits a booking request without collecting payment on the website).
- The live hosted-checkout eligibility path is narrow: selected add-ons or typed special requests preserve the booking but route the primary pay-now choice to Oraya's pending-payment follow-up instead of hosted checkout. Approval-based items are reviewed and charged separately after confirmation.
- `/admin/settings` owns guest-safe public payment behavior such as mode, deposit minimum, guest instructions, manual rails, provider display name, and the guest-visible online-payment-enabled flag. Gateway credentials remain server-only env vars and are never written to Supabase.
- `POST /api/payments/checkout` creates or reuses a canonical booking-linked `payment_request` only after the locked `/api/bookings` POST successfully creates the booking row. Its `/pay/[token]` front door opens the shared `/payments/checkout/[token]?subject=request` experience and the request-scoped session/completion APIs.
- Server-side gateway verification is authoritative for synchronous payment receipt updates; only a production-authenticated, decrypted, signature-verified, replay-protected provider webhook may be authoritative asynchronously. Success redirects are informational only.
- `/api/payments/readiness` surfaces non-secret provider readiness state to admins (configured vs implemented vs checkout-ready, missing requirements).

Phase 16B is still incomplete. The full umbrella mission is [PHASE_16B_PAYMENT_SYSTEM_MISSION.md](PHASE_16B_PAYMENT_SYSTEM_MISSION.md); the CyberSource-specific evidence and blockers remain in [PHASE_16B_PAYMENT_OPERATIONS_AUDIT.md](PHASE_16B_PAYMENT_OPERATIONS_AUDIT.md). CyberSource session creation, transient-token authorization, request-scoped durable attempt safety, JWT/payment MLE, webhook JWE/signature/replay handling, and canonical card routing are present. Production remains fail-closed because the distinct provider-issued keys, Vercel configuration, subscription/URL approval, delivered sandbox webhook evidence, decline/reconciliation validation, and explicit activation are incomplete. Direct send/reminder/reissue/association, Apple Pay, native wallets, saved cards, and settlement operations remain.

The current closeout work around those shipped Phase 16A surfaces is operational:

1. keep WhatChimp production wiring aligned with the shipped backend contract (welcome flow + the two new CTA-substring triggers + the confirmed-guest flow once wired)
2. keep Butler prompt / escalation rules aligned with human operations and with the confirmed-guest disclosure boundary
3. keep secret handling, rotation, and Vercel env posture explicit
4. avoid any accidental Phase 16B payment promises in WhatsApp or website handoff copy
5. **canonical domain hygiene** - the canonical Oraya origin is `https://stayoraya.com` and only `https://stayoraya.com` for everything guest-facing. Any AI / WhatChimp reply that suggests `www.oraya.com.lb` or any other host is a wrong-domain bug; this is documented in [KNOWN_BUGS.md](KNOWN_BUGS.md) and [BUTLER_PLAYBOOK.md](BUTLER_PLAYBOOK.md). One deliberate server-to-server exception (2026-07-03): WhatChimp HTTP API integrations must POST to the direct API host `https://www.stayoraya.com/api/butler/...`, because the bare origin answers those POSTs with a `308` redirect that WhatChimp does not safely follow — see [KNOWN_BUGS.md](KNOWN_BUGS.md) #11 and [DECISIONS_LOG.md](DECISIONS_LOG.md) 2026-07-03. Guest-facing links are unaffected.

## Active objective

Keep the shipped Reserve payment flow stable while Phase 16A operator-side closeout continues and Phase 16B execution work proceeds:

1. **Hosted-checkout execution hardening.** Ensure `POST /api/payments/checkout` and fully production-authenticated provider callbacks remain the only payment execution authority for Reserve bookings after the booking row exists.
2. **Server-side payment truth.** Guest return URLs on `/booking/view/[token]` stay informational; payment state comes from server-side gateway verification and, once the remaining security work is complete, verified webhook updates only.
3. **Confirmed-guest support wiring.** Wire the WhatChimp flow that calls `/api/butler/confirmed-guest-info` for identity-established confirmed guests, including the `butler_checkin_guidance` operator-managed settings row.
4. **Operational readiness.** NetCommerce completed the approved sandbox test. Do not commit signed checkout URLs, capture contexts, merchant values, secrets, or card numbers. Production remains gated by production credentials, JWT request authentication, response MLE, webhook JWE decryption, timestamped digital-signature verification, replay protection, webhook subscription/setup, declined-card validation, deliberate Vercel Production env setup, and explicit human activation. Do not enable the temporary HMAC webhook receiver in production.
5. **`POST /api/butler/flow-submit`** if/when scoped in must not bypass the locked `/api/bookings` POST or promise any payment automation beyond the shipped website hosted-payment flow.

## Just completed

- **Phase 16B-NC webhook security — SHIPPED in PR #112 (2026-08-09, `d7f95b8`).** CyberSource compact-JWE webhook decryption, separate timestamped digital signatures, delivery freshness, durable replay claiming/recovery, and attempt-to-request linkage passed CI, Vercel Preview, live schema smoke tests, and fail-closed endpoint verification before merge.

- **Phase 16B-2B1 — CyberSource JWT/JWS + payment MLE (2026-08-09, PR #109 merged, `ae783b6`).** Session and Payments API requests use short-lived JWT/JWS v2; Payments request and response bodies use the approved MLE contract. Undecryptable/unverifiable post-submission outcomes remain ambiguous and do-not-retry. The change shipped dark and did not activate production charging.

- **Phase 16B-F1/F2/core-F3 foundation — SHIPPED in PR #110 (2026-08-09).** Canonical requests, immutable transactions, provider-event storage, manual cash/transfer rails, partial payments, reversals, booking projections, standalone `/pay/[token]` links, and the `/ops/payments` workspace passed CI, Vercel Preview, desktop/mobile public-link, inactive-link, and unauthenticated-boundary verification before merge.

- **Phase 16B-2A — payment outcome and attempt-state safety (2026-08-06, PR #104).** Unified Checkout completion now distinguishes provider `approved`, explicit retry-safe `declined`, and `unknown` outcomes. HTTP failures, missing/malformed payloads, pending/review/unknown states, and apparent approvals whose server-authoritative amount/currency cannot be verified transition `claimed -> ambiguous` and block another attempt; only HTTP-success `DECLINED` transitions `claimed -> failed` and permits a new attempt. Every attempt mutation now carries an expected-status guard and follows the allowed graph (`claimed -> authorized|recorded|failed|ambiguous`, `authorized -> recorded|failed|ambiguous`, `ambiguous -> recorded|failed`; no transitions leave `recorded` or `failed`). If webhook reconciliation records first, the browser observes `recorded`, skips its booking write, and returns idempotent success. Guest checkout renders the server's safe `message`; reconciliation/unknown paths explicitly say do not retry, and post-completion-request exceptions are not represented as definitely uncharged. PR #104 completes state safety only; it does not implement or approve production webhook security or setup. No schema, dependency, live gate, booking-confirmation, tokenization, or refund-policy change.

- **Plan 4 — payment-operations foundations (2026-07-24/25, PR #94 merged).** Phase 1: refunds are MANUAL-FIRST by explicit decision — the admin action is "Record manual refund", requires the NetCommerce Business Center refund/transaction reference (400 without it), persists it to `bookings.refund_provider_reference` (additive human-run `sql/plan4-refund-provider-reference.sql`), and the operator runbook is [REFUND_RUNBOOK.md](REFUND_RUNBOOK.md) (KNOWN_BUGS #15 resolved-by-policy). Phase 2 added a reconciliation core behind a dedicated fail-closed raw-body HMAC receiver; that receiver is temporary compatibility code, not final CyberSource production verification. Phase 3 added the fail-closed `payments_live_enabled` switch and admin toggle endpoint. Those foundations do not make checkout production-ready: the placeholder webhook configuration must not be enabled or subscribed in production, and the production authentication, encryption, signature, replay, and subscription work remains open. See DECISIONS_LOG 2026-07-25 (three entries; production-readiness wording superseded by this correction).

- **Plan 3 — remediation follow-through + stack upgrade (2026-07-24, PRs #90–#93 merged).** Durable payment-attempt idempotency for Unified Checkout completion: the `payment_attempts` ledger (human-run `sql/plan3-payment-attempts.sql`) is claimed atomically before any provider call, a deterministic merchant reference ties retries to one operation, and every conditional booking update is row-count verified (KNOWN_BUGS #14 resolved; ambiguous outcomes block until reconciled). `GET /api/health` returns 200/503 on required production config (names only, never values) and all email senders log a structured `[email-config-missing]` line (KNOWN_BUGS #2 resolved). The stack moved to **Next 16.2 (Turbopack build) / React 19.2**, eslint 9 flat config, react-day-picker 9, async request-API codemod; `npm audit` 0 vulnerabilities. 26 already-merged remote branches were deleted.

- **Health-check remediation waves (2026-07-23/24, PRs #85–#89 merged).** Security-critical: admin password verified against a scrypt hash in `settings.admin_password` ([lib/admin-password.ts](../../lib/admin-password.ts)), full admin password-change flow (`/api/admin/change-password`), forgot-password recovery via `ADMIN_RECOVERY_EMAIL` (`/api/admin/recovery/request|reset` + [lib/send-admin-recovery-email.ts](../../lib/send-admin-recovery-email.ts)), CyberSource authorized amount/currency verified before recording payment, webhook `set_paid` double-count fixed. Hardening: outbound fetch timeouts/size caps/https-only, generic errors on public routes, member booking-modification hardening. Infra: CI workflow + shared `npm test` script, edge-case tests for money/token-critical libs, dependency updates. Rendering refactors (PR #88): homepage is a server component with the [components/HomeClient.tsx](../../components/HomeClient.tsx) client island; villa/homepage covers use `next/image`; BookingsTable split into components. See DECISIONS_LOG 2026-07-23 (six entries).

- **Phase 16B corrective payment-state consistency (2026-07-17, fresh branch after merged PR #81).** A complete payment-code audit confirmed that `bookings.status = pending` plus `payment_status = paid_in_full` is intentional: payment never auto-confirms the stay. The defect was presentation coupling — static booking-status copy separately claimed "No payment required yet" or "Payment received / booking confirmed" while the dedicated payment panel showed authoritative payment truth. Booking-status copy is now payment-neutral, guest payment projection lives in pure [lib/payments/guest-presentation.ts](../../lib/payments/guest-presentation.ts) with a focused state matrix, `card_manual` displays through the shared "Debit / Credit Card" label, `/book` pre-payment copy matches pay-before-confirm behavior, and public checkout failure surfaces no longer echo provider/configuration detail. No schema, locked booking/admin/auth/token/email surface, production enablement, webhook, refund, or booking-confirmation behavior changed. The known duplicate-charge/idempotency, webhook/MLE, declined-vector, settlement, and refund-operation gaps remain production blockers.

- **Phase 16C — automatic WhatsApp Arrival Guide webhook dispatch (2026-07-17, ships dark).** When a STAY booking becomes confirmed through either authoritative writer, [lib/whatsapp/confirmed-stay-notification.ts](../../lib/whatsapp/confirmed-stay-notification.ts) fail-closed POSTs one safe payload (villa, dates, reference, digits-normalized phone, `arrival_guide_url`) to the WhatChimp Webhook Workflow URL configured in `WHATCHIMP_CONFIRMED_STAY_WEBHOOK_URL`, so WhatChimp sends the approved Utility Template `oraya_arrival_guide_confirmed` (renamed 2026-08-01; Meta-approved and live in the WhatChimp Webhook Workflow "Confirmed booking" — the Production env URL is the remaining on-switch). At-most-once via the atomic `bookings.whatsapp_confirmation_sent_at` claim (additive human-run migration `sql/phase-16c-whatsapp-confirmation-tracking.sql`); event inquiries, non-production, missing phone, finished stays, and mint failures skip safely; email behavior unchanged; **no PIN/access delivery (Phase 16D boundary intact); no Meta/WABA code or credentials in the repo**. Focused tests 22/22. See DECISIONS_LOG 2026-07-17. Activation = human steps: run the migration, then set the Production URL after Meta approval + WhatChimp workflow creation.

- **Corrective follow-up — member-profile booking-view TTL (2026-07-15).** PR #77's profile mint no longer derives token expiry from `check_out` (which left historical bookings with already-expired tokens and parsed a date-only field via `new Date()`). `POST /api/profile/booking-view` now calls `createActionToken(bookingId, "view")` with the locked helper's default temporary TTL; authenticated owners can remint a fresh relative `/booking/view/[token]` path at any time while they still own the booking. Email checkout-day expiry is unchanged. See DECISIONS_LOG 2026-07-15 follow-up.

- **Phase 16C Stage 4B — Butler confirmed-guest boundary returns `arrival_guide_url` (2026-07-15).** `POST /api/butler/confirmed-guest-info` now includes the permanent WhatChimp field contract **`arrival_guide_url`** — `https://stayoraya.com/arrival/<signed-view-token>` (existing signed `view` token, checkout-day expiry, minted by the new shared [lib/arrival-guide-link.ts](../../lib/arrival-guide-link.ts)) — **only** for identity-established **confirmed** bookings with a usable `check_out` and a supported villa; `null` on every refusal branch. The confirmed `safe_message` ends with "Your Arrival Guide is ready here: <arrival_guide_url>". **No outbound WhatsApp sender, no WhatChimp artifact edits, no PIN/access delivery** — operator maps `arrival_guide_url` in the confirmed-guest flow later (BUTLER_PLAYBOOK operator note). Focused `node:test` suite 9/9. See DECISIONS_LOG 2026-07-15 (Stage 4B). Phase 16C is still **not** complete: operator-side WhatChimp wiring and Phase 16D access automation remain.

- **Phase 16C Stage 4A — admin "Copy Arrival Guide link" for manual WhatsApp sending (2026-07-15).** New admin-auth route `GET /api/admin/bookings/[id]/arrival-link` mints the personalized `/arrival/<signed-view-token>` URL (existing signed `view` token, checkout-day expiry — same contract as the confirmed email) for **confirmed bookings only** (pending/cancelled refused, no link minted), and the admin bookings console gains a confirmed-only "Copy Arrival Guide link" action that copies the URL to the clipboard for manual WhatsApp pasting. **Manual workflow only** — no outbound WhatsApp sender, no WhatChimp field, no PIN/access delivery. See DECISIONS_LOG 2026-07-15 (Stage 4A). Phase 16C is still **not** complete: automated WhatsApp delivery and Phase 16D access automation remain future stages.

- **Member profile → canonical booking-view link (2026-07-15).** `/profile` "My Bookings" cards now expose a clear **View booking** action. The client calls `POST /api/profile/booking-view` (Bearer member auth); the server verifies `bookings.member_id` matches the authenticated user, then mints a fresh signed view token via the locked `createActionToken` helper (import only) and returns a relative `/booking/view/<encoded-token>` path. Arrival Guide access remains gated solely by the existing confirmed-only behavior on `/booking/view/[token]` → `/arrival/[token]`. Modify / Cancel / WhatsApp controls are preserved as sibling actions (no nested interactive elements). No changes to locked booking, payment, email, auth, token, or Arrival Guide surfaces.

- **Phase 16C Stage 3 — confirmed-email Arrival Guide delivery (2026-07-15).** The confirmed booking email now carries exactly one "Open your Arrival Guide" CTA to `/arrival/<signed-view-token>`, reusing the email's own checkout-day-expiry view token (no new token minted). Confirmed email only — pending/cancelled/payment/event/admin/feedback emails unchanged; no PIN/access claims ("Access details are shared by Oraya before arrival"). See DECISIONS_LOG 2026-07-15 (Stage 3). Phase 16C is still **not** complete: WhatsApp/admin delivery (Stage 4) and Phase 16D access automation remain future stages.

- **Phase 16C Stage 2 — private Arrival Guide route (2026-07-15).** `/arrival/[token]` ships as the private mobile Arrival Guide: reuses the signed booking-view token (locked helper imported only), renders guest name / stay dates / booking reference / villa-specific guide **for confirmed bookings only** (pending → neutral locked state; cancelled/invalid/expired → safe neutral states), `noindex`, linked only from the confirmed-booking state of `/booking/view/[token]`. **No gate/door PINs or access codes render** — access delivery remains Phase 16D. See DECISIONS_LOG 2026-07-15 (Stage 2). Phase 16C is still **not** complete: email delivery (Stage 3), WhatsApp/admin delivery (Stage 4), and Phase 16D access automation remain future stages.

- **Phase 16C Stage 1 — public House Book + Explore / Living List routes (2026-07-15).** Four public guest-guide routes shipped as repo-native React from the PR #73 Claude Design package: `/house-book/mechmech`, `/house-book/byblos` (9-page A4 printable House Book; route-scoped print CSS; no guest-specific data, no merge fields, no access codes) and `/explore/mechmech`, `/explore/byblos` (villa area guides — the **permanent destination of the printed House Book QR codes**; see DECISIONS_LOG 2026-07-15). Minimal links added on the homepage and villa pages. Phase 16C is **not** complete: private Arrival Guide routes, email/WhatsApp delivery, admin send/copy actions, and Phase 16D access automation remain future stages.

- **✅ LOCKED 2026-07-09 — Phase 16A - Structured date fallback: the Normalize Dates ladder replaces the "send both dates together" retry (merged PR #69 `92c1441`, production-verified).** Per David's hybrid-ladder clarification the whole v5.5 retry layer is deleted. Date recovery now keeps ONE combined follow-up → refine (`8101`, the only refine call left), then a structured ladder built on the **existing tenant integration 7458 "Oraya Normalize Dates"** (fixed body reads `oraya_check_in_text` 58017 / `oraya_check_out_text` 58018): single-date questions save the guest's raw words and call ND — check-out-only when the check-in is held, two chained one-by-one questions when nothing is held, and — for a check-out-only gap — the follow-up `#426` REBOUND to 58018 feeding ND FIRST (fixes the split-turn "11 july" reply the refine-first wiring clobbered), with one structured re-ask. Every outcome ends on a one-button **"Continue"** Interactive (field 58532) whose press rejoins the intake through the dates-recovered `#1150` (4-way) or dates-pending `#1200` (3-way) hub Text; H_R hard-wires the dated summary, H_P the undated one (no name question, no date-escalation tail). Zero WhatChimp integration edits for the flow — only two ADDITIVE 7466/8101 mapping rows (`extracted_text.check_in`→`oraya_check_in_text`, `…check_out`→`oraya_check_out_text`, same class as the bedroom row) so an already-extracted ISO date reaches the ND body. **Backend UNTOUCHED** — new `lib/butler/normalize-dates.test.mts` (9/9) pins the ND behaviors the flow relies on. New canonical `Oraya_natural_intake_v6.txt`: **192 nodes / 247 output connections / 22 Interactive / 62 controls / 15 terminals / ZERO redraws**, SHA-256 `4383A12F4EEE2E1E721F94BB03DA584B0383D3F47A69BC77C48F659C2E841659`; hubs `{860:24, 865:4, 930:15, 938:12, 1150:4, 1200:3}` (bedroom-skip stages 8→5 as the retry-layer branches are gone). The 258-node bedroom-skip artifact's import survival was RE-PROVEN at full scale (TEST-bot re-export pinned as round-trip-6, SHA `BEB37863…C707` — all 344 edges, 42/24/18/7-way hubs intact). Validator strict 0/0 (new `normalize-dates-placement` check); simulator 53/53 (N01 one-by-one target UX, N02 check-out-only, R04 split-turn first-try, D01–D06 pending via both ND null semantics, F01/F02 HTTP-failure); tooling 42/42; extractor 42/42. **Human gate: interactive acceptance on a fresh disposable TEST bot incl. confirming integration 7458 exists tenant-side (older flows referenced a superseded `7102`), the two additive 7466/8101 rows, the split-turn "11 july" first-try recovery, and the "Continue" → hub connectors** (DECISIONS_LOG 2026-07-08 "Structured date fallback"; KNOWN_BUGS #10 follow-up). **Follow-up (2026-07-08, commit `0ad7dbf`): the "Backend UNTOUCHED" note above no longer holds for the branch.** A separate live test of the full sentence "i want to book mechmech from july 10 to july 15 …" returned null dates — the "to" in "want to" is a range connective, not filler, so it stranded and made the splitter split at the wrong "to". `lib/butler/extract-stay-intent.ts` (`cleanResidue`) now drops leading lead-in tokens up to the first date-anchoring token; **extractor 42→48**, `tsc`/`build` exit 0, flow artifact/generator/tooling unchanged. Full-message live verification must run against the PR's **Vercel Preview** (`https://oraya-bdmj34w73-staleen-2375s-projects.vercel.app`) or a merged production deploy — production does not yet carry the fix (DECISIONS_LOG / KNOWN_BUGS #10 2026-07-08 backend-extractor follow-ups).

- **Phase 16A - Bedroom-skip hotfix: an extracted bedroom label skips the bedroom question (2026-07-08, branch `claude/v6-bedroom-skip` on merged PR #67).** PR #67 merged and deployed (`7ee01a4e`); the post-merge live test confirmed dates/villa/guests/lead/prefill all work — the one remaining gap was the bedroom question rendering even when the guest volunteered the count. Each of the 8 bedroom-stage entries now feeds a **bedroom-known gate** (equality any-match on the three approved labels EXACTLY; `""`/`"null"`/whitespace/invalid → ask as before, keeping the stale-bedroom reset semantics); skip tails are branch-local (villa-gate clone + Lead Submit + dated/undated summary hard-wired by branch reachability; only the clicked-guest branch keeps a date-branch Condition), and the 8 branch-local villa Interactives rejoin the shared villa-ack `#938` via their buttons (hub 2-way → **18-way**, the proven control → Text class at a new scale — human-gated). The extractor already normalizes singular "2 bedroom" (new regression test; no backend change). New canonical `Oraya_natural_intake_v6.txt`: **258 nodes / 344 output connections / 24 Interactive / 91 controls / 21 terminals / ZERO redraws**, SHA-256 `8A356B6F508AF158274576521958F98FA2992A06560E23A65CB1558FBBB51A6D`; hubs `{860:42, 865:7, 930:24, 938:18}`; existing 200 nodes keep the operator layout, the 1000-range skip nodes carry computed positions. Validator strict 0/0; simulator 62/62 (L01 = the exact live message with **ZERO questions**; K01–K17 bedroom-skip matrix; T03 stale-bedroom re-ask); tooling tests 40/40; extractor 42/42. **Human gate: interactive acceptance on a fresh disposable TEST bot incl. C1 (zero questions on the live message) + C1a (skip/ask matrix) + the 18-way `#938` fan-in inspection** (DECISIONS_LOG 2026-07-08 "Bedroom-skip routing"; KNOWN_BUGS #10 follow-up).

- **Phase 16A - Live runtime repair: presence-based date/villa routing + bedroom-phrase extraction + operator layout baseline (2026-07-04).** The first live conversation on the fresh bot failed on a runtime data contract (structural round trip had PASSED): "mechmech july 10 to july 11 for 4 people 2 bedrooms" produced a dated summary with a BLANK check-out and a blank secure link. Repairs: (1) the extractor stripped-and-extracts a volunteered bedroom phrase (it was corrupting date parsing — the live message now extracts both dates + `bedroom_count` "2 bedrooms"; new `extracted_text.bedroom_count` → `oraya_bedroom_count` mapping; extractor tests 41/41); (2) every date/villa condition now routes on **presence** (`contains "-"` / `contains "Villa"` — `""` from a misbound `extracted.*` mapping, `"null"`, and whitespace all read as missing; a dated summary requires BOTH real dates; validator `date-presence` check; simulator blank/half-date scenarios, 44/44); (3) blank secure link traced to the tenant's missing `prefill_url` mapping — backend proven, checklist troubleshooting added. The operator's hand-tuned layout is adopted (fixture `roundtrips/…roundtrip-5.layout-baseline.txt` SHA `7DDF81E4…3156`; positions only — its accidental 7459/7460 lead rebinds are test-blocked). New canonical SHA `5D39B6580A9AB784C31811D1255F4AD6509581F88635006FB6AECE7272A56B64` (totals unchanged: 200/270/16/75/12/ZERO redraws); tooling tests 39/39. **Human gate: repeat acceptance incl. C1 = the exact live message → both dates + non-empty link; C4 = half-date ask + dates-pending** (DECISIONS_LOG 2026-07-04 "Live runtime repair"; KNOWN_BUGS #10 follow-up).

- **Phase 16A - Dates-pending continuation: failed final date retry now continues the interactive intake + secure website handoff; date-escalation tails deleted (2026-07-04).** After ONE date follow-up + ONE retry, a failed final retry no longer escalates: the flow continues into the guest-count missing/extracted branch, collects bedrooms/villa with the existing Interactive controls, submits the lead with the date fields as the literal `"null"` (backend stores NO date — new focused suite `lib/butler/leads-absent-dates.test.mts` 4/4; prefill URL minted from the lead id alone), and ends on a **date-aware summary**: dated variant shows both dates; undated variant shows "Dates: Please choose them using the secure link below." (never `null`, blank hashtags, or a half-captured date) — the guest picks dates on `/book`. **No name question; nodes 640–643/700–703 deleted; large-group escalation unchanged; the `Rows → #865 → #466` overflow repair untouched.** New canonical `Oraya_natural_intake_v6.txt`: **200 nodes / 270 output connections / 16 Interactive / 75 controls / 12 terminals / ZERO redraws**, SHA-256 `4D032FEC5209EF6685BD84BDA0980EBAAD7AB8BF8031AD9B8DD96BCB9BE1E46B`; hubs `{860:42, 865:7, 930:24, 938:2}` (42-way guest-ack fan-in exceeds the proven 30-way scale — same class, human-gated). Validator strict 0/0 (dated/undated summary contract + refine-continuation checks), simulator 40/40 (D01–D07 dates-pending matrix), tooling tests 38/38. **Human gate: interactive acceptance procedure incl. the rewritten C4 dates-pending scenario** (DECISIONS_LOG 2026-07-04 "Dates-pending continuation"; KNOWN_BUGS #10 follow-up).

- **Phase 16A - Round trip #4: narrow failure isolated `Rows → User Input Flow` as an import-dropped class; five "More than 6" routes repaired via one shared acknowledgement Text (2026-07-04).** The operator's acceptance import of the 161-node interactive candidate dropped **exactly 5 of 211 connections — the five "More than 6" `rowOutput → #466 userInputFlowInput` edges** — stranding the large-group subtree, while **all three control → Text postback hubs survived at full fan-in (30/18/2-way)**: import survival of serialized control → Text convergence is now PROVEN; the evidence isolates the target type, not postback merges in general (fixture `artifacts/whatchimp/roundtrips/Oraya_natural_intake_v6.roundtrip-4.saved-reexport.txt`, SHA-256 `9C9EAD4C…C031`). Smallest repair shipped: the five rows converge on ONE shared "Got it 😊" Text `#865` carrying the single serialized edge into `#466`; exact-count/team-review/lead/terminal path byte-preserved. New canonical `Oraya_natural_intake_v6.txt`: **162 nodes / 212 output connections / 12 Interactive / 55 controls / 10 terminals / ZERO redraws**, SHA-256 `F846CBFA51FBA0452DB17376123C205FC6D73DFC1F553EA3CC40CC5B2974CB47`; hubs `{860:30, 865:5, 930:18, 938:2}` — every hub a Text; control→User Input Flow is now rejected by the generator gate and the validator `interactive-contract` check, and a missing declared hub is a validation error. Validator strict 0/0, simulator 34/34, tooling tests 37/37. **Human gate: repeat the interactive acceptance procedure — import → no warning → inspect all five "More than 6" connectors → save → close → reopen → export → comparator PRESERVED + strict validator exit 0; round-trip compatibility may not be claimed until that new export passes** (DECISIONS_LOG 2026-07-04 "Round trip #4"; KNOWN_BUGS #10 follow-up).

- **Phase 16A - Interactive-controls v6 rebuild: buttons/rows save custom fields directly; ZERO operator redraws (2026-07-04).** The operator's authenticated saved/reopened re-export (pinned at `artifacts/whatchimp/roundtrips/Oraya_natural_intake_v6.button-evidence.saved-reexport.txt`, SHA-256 `1D4A5E3D…11EB`) proved Inline Buttons bind directly to custom fields and converge forward normally — and exposed the banned Start-a-Flow+connector self-restart hazard (`#776`). The canonical `Oraya_natural_intake_v6.txt` was rebuilt on Interactive controls: guest count = one-click 7-row list (1–6 + "More than 6", `custom_57693`), bedrooms = 6 stages × 3 buttons (`custom_69114`), villa = 2 canonical buttons (`custom_57698`); extracted values skip their question; **confirmation, Edit loop, handoff choice, and happy-path name ask removed** (operator authority — escalation tails keep their name question); completion = Lead Submit `6961` → summary; `7459` unreferenced; no WhatsApp-side capacity validation (`/book` is the authority). New candidate: **161 nodes / 211 output connections / 12 Interactive / 55 controls / 10 terminals / ZERO redraws**, SHA-256 `F28106948012125CFB3324CEEDCDB414C8B2C0B33BF4B1179D827634550669E7`; validator strict 0 errors / 0 warnings (new `interactive-contract` + postback-hub checks), simulator 34/34 (full click coverage), tooling tests 35/35; `V6_REDRAW_CHECKLIST.md` is now a click-verification matrix ("ZERO connections to draw"). **Human gate: the interactive acceptance procedure in `V6_ROUNDTRIP_CHECKLIST.md`** — fresh disposable bot → import (no warning, no loose connectors) → click every control + inspect all three fields (stored value == label, exactly-once advance) → save/close/reopen retest → export → comparator PRESERVED + strict validator exit 0 → full conversation → saved lead — then live checklist C (DECISIONS_LOG 2026-07-04; KNOWN_BUGS #10 follow-up).

- **Phase 16A - Round trip #3 HALTED → corrected invariant (one inbound TOTAL per Condition) + full Condition-clone-cascade v6 candidate shipped (2026-07-03).** The operator's round trip #3 on the 181-node candidate failed: the import/open raised the infinite-loop warning (Text `#603` left disconnected from `#602`), and the 3 Condition-destination redraw items (#5 `#603→#602`, #22 `#659→#654`, #24 `#664→#663`) were each rejected — proving the corrected rule **`maxInboundPerCondition: 1` (any source type)** and that unused-input probes don't prove drawability against connected destinations. The canonical `Oraya_natural_intake_v6.txt` was rebuilt with the **full cascade** (+5 clones ids 766–770; 21 total 750–770): every Condition has exactly one inbound edge, **no redraw targets a Condition**, and the **13 declared merge points** (all User Input Flow wrappers or Texts) are repaired by **exactly 39 operator-drawn connections** (34 Condition→User Input Flow, 2 Text→User Input Flow, 3 Condition→Text). New candidate: **186 nodes / 224 output connections / 14 terminals**, SHA-256 `1C7335ED49C7F9F7738B58CCDA4BA340543A9354002FAC2F9FBEC672784EDD48`; validator `--strict-binding` exit 0 (0 errors / 0 warnings; check renamed `condition-inbound-limit`), simulator 50/50, tooling tests 33/33. The failed 181-node candidate is pinned byte-exact as `artifacts/whatchimp/roundtrips/Oraya_natural_intake_v6.roundtrip-3.failed-candidate.txt` (SHA `AB456A89…AB0C`) with a regression test; evidence in `artifacts/whatchimp/roundtrips/ROUNDTRIP_3_FINDINGS.md`. **Human gate: round trip #4 on a fresh disposable bot — import → 39 redraws per `V6_REDRAW_CHECKLIST.md` (stop and report if ANY is refused) → save → close → reopen → export → comparator PRESERVED exit 0 + validator exit 0 — then live checklist C** (DECISIONS_LOG 2026-07-03 "Round trip #3"; KNOWN_BUGS #10 follow-up).

- **Phase 16A - Corrected editor rule + Condition-clone-cascade v6 candidate shipped (2026-07-03).** The operator's live probe matrix corrected the round-trip-#2 conclusion: the editor rejects only a **second Condition-source parent** on a destination Condition (rule: at most one Condition-source parent per Condition); six connection operations are live-proven, including Condition→Condition as a first Condition-source parent. The canonical `Oraya_natural_intake_v6.txt` was rebuilt with the operator-directed, behavior-preserving Condition-clone cascade: 16 guest-invisible Condition clones (ids 750–765) absorb every extra Condition-source parent via serialized import-kept edges, leaving **15 declared merge points** repaired by **exactly 34 operator-drawn connections** — every one a live-proven operation, zero drawn Condition→Condition. New candidate: **181 nodes / 214 output connections / 14 terminals**, SHA-256 `AB456A895221A46DE289EDA054DB9142B4D3F7D0A1892A3FFBEAFF999346AB0C`; validator `--strict-binding` exit 0 (0 errors / 0 warnings; new graph-wide `condition-parent-limit` check), simulator 50/50, tooling tests 32/32, generator refuses to emit any plan that violates the corrected rule. The halted 165-node candidate is pinned byte-exact as `artifacts/whatchimp/roundtrips/Oraya_natural_intake_v6.roundtrip-2.halted-candidate.txt` (SHA `0066192D…3A39`) with a regression test. **Human gate: round trip #3 on a fresh disposable bot — import → 34 redraws per `V6_REDRAW_CHECKLIST.md` → save → close → reopen → export → comparator PRESERVED exit 0 + validator exit 0 — then live checklist C** (DECISIONS_LOG 2026-07-03 "Corrected editor rule"; KNOWN_BUGS #10 follow-up).

- **Phase 16A - Round trip #2 HALTED: the WhatChimp editor refuses Condition → Condition connections; hybrid redraw plan not operator-executable (2026-07-03).** The operator attempted redraw item #1 on a fresh disposable bot and the editor rejected the connection (*"This will make an infinite loop. Place a button/list/section/interactive between these two nodes."*). Machine audit: **11 of the 18 planned draws are Condition → Condition** (items #1–4, #6–8, #12, #14–16); the other 7 type pairs are unproven — and the v5.5 `#440` 5-parent merge previously cited as drawability evidence is itself Condition → Condition ×5 (export survival ≠ present-editor drawability). The candidate `Oraya_natural_intake_v6.txt` and `V6_REDRAW_CHECKLIST.md` are **not approved for human testing**. Rules encoded before any regeneration: profile `editorRejectedDrawPairs`/`editorProvenDrawPairs`, validator `redraw-drawability` check (candidate fails **by design**: 11 errors + 7 warnings), regenerated checklist with ⛔ halt banner (artifact bytes unchanged, SHA `0066192D…3A39`), tooling tests 31/31. Evidence + audit + quantified behavior-preserving rebuild (Condition-clone cascade: +21 nodes, 39 draws onto interactive/Text nodes, guest behavior byte-identical — contingent on probes) in `artifacts/whatchimp/roundtrips/ROUNDTRIP_2_FINDINGS.md`. **Gate: operator runs the 5-probe editor drawability matrix (findings §6), then a human decision picks the rebuild architecture; no interactive-node workaround without explicit UX approval** (DECISIONS_LOG 2026-07-03 "Round trip #2"; KNOWN_BUGS #10 follow-up).

- **Phase 16A - Option A hybrid v6 rebuild shipped: import + 18-edge operator redraw (2026-07-03).** Following the operator's Option A decision, the v6 generator/artifact were rebuilt to the hybrid architecture: small linear tails (10 escalation tails, the Edit large-group review, a complete Edit-handoff subtree with its own Lead Submit nodes and terminals) are cloned branch-locally as single-parent, while the 11 irreducible hub merges (`440:5, 470:4, 490:2, 602:2, 616:2, 654:2, 660:2, 663:2, 676:2, 690:4, 694:2`) stay convergent and are declared in the profile's `importGraphContract.approvedHubMerges` — after import the operator draws **exactly 18 connections** per the machine-generated `artifacts/whatchimp/V6_REDRAW_CHECKLIST.md`. New canonical `Oraya_natural_intake_v6.txt`: **165 nodes, 182 output connections**, SHA-256 `0066192D4487ED1AC8A95B00E22DEF8B754B1FB458E0025B178E8B125C913A39`. The un-repaired import is safe by construction (first-listed hub edges = the full happy path; opening-question `/book` safety link; no terminal claims confirmation). Evidence: validator `--strict-binding` exit 0 (0 errors / 0 warnings; `single-parent-contract` accepts only declared hubs at exact inbound counts), simulator 50/50 with all 14 terminals covered, tooling tests 30/30 incl. byte-for-byte generator reproducibility; the pre-hybrid candidate is pinned at `artifacts/whatchimp/roundtrips/Oraya_natural_intake_v6.roundtrip-1.import-candidate.txt`. **Human gate:** round trip #2 on a fresh disposable bot — import → 18 redraws → save → close → reopen → export → `compare-whatchimp-roundtrip.mjs` PRESERVED exit 0 + validator exit 0 — then live checklist C (DECISIONS_LOG 2026-07-03 "Option A hybrid rebuild"; KNOWN_BUGS #10 follow-up).

- **Phase 16A - Authenticated WhatChimp round trip #1: v6 candidate proven NOT import-safe (2026-07-03).** The operator ran the full import → save → close → reopen → export sequence on a test bot with the canonical `Oraya_natural_intake_v6.txt`. All 118 nodes and every binding survived, but WhatChimp kept only the **first serialized connection per input socket** and silently removed the artifact's 32 convergence edges — all 16 multi-parent nodes were reduced to one parent, creating 16 unintended terminals and breaking every bedroom-capacity route, every escalation except the first date failure, the entire Edit path, and the date-recovery merges (validator on the re-export: 96 errors; simulator: 6/42). The evidence re-export is preserved byte-for-byte with the complete machine-derived lost-edge table in `artifacts/whatchimp/roundtrips/ROUNDTRIP_1_FINDINGS.md`; a new comparator (`scripts/compare-whatchimp-roundtrip.mjs`) machine-checks any future round trip; the validator's new `single-parent-contract` check now rejects convergent import candidates (the current artifact fails it with exactly 16 violations, by design). A pure single-parent rebuild preserving current behavior is quantifiably infeasible (exact unroll: 492,864 nodes); editor-drawn merges are export-proven to survive (v5.5 `#440`, v4.3.3), so the structural direction — bounded operator redraw of hub merges vs approved behavior reduction to a pure tree — is an **operator decision, pending** (DECISIONS_LOG 2026-07-03; KNOWN_BUGS #10 follow-up).

- **Phase 16A - Natural stay intake v6 candidate + deterministic flow tooling (2026-07-02).** The WhatChimp natural-intake flow is now a generated, machine-validated artifact: `Oraya_natural_intake_v6.txt` (import-ready candidate; NOT yet round-trip verified in WhatChimp), built from the operator's v5.5 export by `scripts/generate-whatchimp-v6.mjs` and gated by `scripts/validate-whatchimp-flow.mjs` (semantic validator — exits 1 with 60 errors on v5.5 at the current head, 0 on v6) plus `scripts/simulate-whatchimp-flow.mjs` (deterministic conversation scenarios with stubbed fixtures, incl. a fault-injection matrix — 42/42 at the current head). Adds the exact-guest (1–8 + "More than 8") and three-button bedroom steps mirroring `/book`'s `BEDROOM_CAPACITY`, complete lead-submitting escalations, a complete forward-cloned Edit path, and stale-field safety via the additive `extracted_text.*` response mirror on `POST /api/butler/normalize-stay-intent`. `/api/butler/prefill` additively returns a validated `bedroom_count`; `/book` hydration prefers the explicit preference. The real `oraya_bedroom_count` field id (`69114`, operator-created 2026-07-03) is emitted directly by the generator — the single canonical import file `Oraya_natural_intake_v6.txt` is fully bound with no placeholder and no second binding step — and the 9 question edges the operator observed as loose after import are rebuilt through export-proven acknowledgement Texts (validator `question-transition` check + simulator node-level path assertions — see DECISIONS_LOG 2026-07-03). Outstanding: authenticated import/save/close/reopen/re-export round trip, live checklist (`artifacts/whatchimp/V6_ROUNDTRIP_CHECKLIST.md`), and the shared-integration audit before editing 7466/8101 mappings (see KNOWN_BUGS #10). See DECISIONS_LOG 2026-07-02.

- **Phase 16B - PR #64 merged after successful NetCommerce sandbox testing (2026-07-02).** The approved-payment validation loop passed, the official NetCommerce seal is present, and saved-card consent/tokenization is disabled. The temporary Preview QA bypass and booking auto-confirm exception were removed after testing. Normal payment completion leaves `bookings.status` pending. Production checkout remains code-gated off.

- **Phase 16B - NetCommerce saved-card omission request (2026-06-22; scope clarified 2026-08-09).** NetCommerce confirmed sandbox testing was successful and asked Oraya to omit the "Save card for future payment" option before account activation. The merged implementation keeps the current Unified Checkout launch one-time only: no reusable payment tokens, saved-card consent storage, credentials-on-file, recurring/merchant-initiated charging, or token management UI. That launch constraint remains in force. The 2026-08-09 owner decision places a separately designed, tokenized saved-card capability later inside the Phase 16B umbrella; it does not turn it on now. No further NetCommerce contact, answer, or approval is a prerequisite for the already completed one-time-card sandbox work.

- **Phase 16A - Natural WhatsApp stay intake — technical gate passed (2026-06-05, Batch 2).** WhatChimp confirmed able to call `POST /api/butler/normalize-stay-intent` and map nested `extracted.*` response fields (`extracted.check_in`, `extracted.check_out`, `extracted.villa`, `extracted.guest_count`). Endpoint reachability and nested field mapping are architecture-validated. **Production flow migration still pending:** the `oraya_stay_text` custom field, new trigger, `User Input Flow Single` capture node, HTTP API call, and response-driven branches have not yet been confirmed as wired; the old four-step intake is not yet retired on the production tenant.

- **Phase 16A - Natural WhatsApp stay intake backend (2026-06-05).** New `POST /api/butler/normalize-stay-intent` extracts structured stay fields (`check_in`, `check_out`, `nights`, `villa`, `guest_count`) from a single free-text `stay_text` reply. Pure extraction; never reads/writes Supabase, never checks availability, never persists a lead. Date arithmetic is delegated to the existing `normalizeStayDates` helper so `YYYY-MM-DD` discipline stays in one place. Villa detection covers the same aliases the existing `lib/butler/villa.ts` resolver recognizes; guest-count detection is a small regex set. Missing-field fallbacks (planned for the operator-side flow in batch 2) are buttons-only for villa (Mechmech / Byblos) and number buttons 1–8 for guest count. 31 unit tests cover the headline combined-message cases plus hostile-input safety. `/api/butler/normalize-dates` is unchanged. `tsconfig.json` gained `allowImportingTsExtensions: true` (single-line) so the helper's explicit `.ts` extension works in both Next.js and Node's TS-strip ESM loader. No schema change, no new env var, no new dependency, no locked-API touch. See [DECISIONS_LOG.md](DECISIONS_LOG.md) 2026-06-05 "Natural WhatsApp stay intake" entry. **Outstanding:** operator-side WhatChimp wiring (batch 2 — new `oraya_stay_text` custom field + new trigger + new prompts + response-driven branches; documented in [BUTLER_PLAYBOOK.md](BUTLER_PLAYBOOK.md) "Natural stay intake (Phase 16A)").

- **Phase 16A - Butler handoff auto-advance + bedroom derivation (2026-06-05).** Two behaviour gaps in the WhatsApp → website handoff are closed in [app/book/page.tsx](../../app/book/page.tsx): (1) `applyButlerPrefill` now derives `bedroomCount` from the hydrated `sleeping_guests` value using the inverse of `BEDROOM_CAPACITY` (`≤ 2 → 1 bedroom`, `≤ 4 → 2 bedrooms`, `≤ 6 → 3 bedrooms`), with a guard that preserves any manual bedroom selection already made; (2) a new `useEffect` auto-advances from Step 1 to Step 2 / Stay Setup once `butlerPrefillReady` and `availabilityReadyForSelection` are both true, dates are valid, and the session is identified as a butler handoff. The existing `reserveAutoAdvanceSuppressedRef` / `reserveAutoNavigatedRef` guards ensure the advance is one-shot and suppressed after an explicit Back. `transitionStep1To("request")` is reused for the advance (it already handles scroll-to-top). No API, schema, auth, or payment change. `npx tsc --noEmit` and `npm run build` clean. See [DECISIONS_LOG.md](DECISIONS_LOG.md) 2026-06-05 entry.

- **Phase 16B - Step 3 dual-CTA refinement (2026-05-23, [apps#58](https://github.com/Staleen/oraya-web/pull/58)).** The Step 3 secondary action is now a real lower-priority button. The flow previously rendered the secondary path as a plain text link ("Prefer to reserve and pay later? Submit booking request"); it now renders as an outline button with a transparent background, thin gold border, and gold text labelled **"Reserve now, pay later"** that reads as visibly lower priority than the solid gold **"Continue to secure payment"** primary CTA. The apologetic "Online payment setup is in progress." copy was dropped from the page-level default; the neutral fallback at the throw site ("Online payment is not available for this booking right now.") applies when no admin-configured message is returned.

- **Phase 16B - Booking flow consolidated to three steps (2026-05-23, [apps#56](https://github.com/Staleen/oraya-web/pull/56)).** [app/book/page.tsx](../../app/book/page.tsx) now ships a three-step Reserve flow with labels `["Villa & Dates", "Stay Setup", "Review & Guest Details"]` (verified at app/book/page.tsx:824). A visible Step 4, a standalone Guest Details step, and a four-step booking journey were all explicitly rejected. Step 3 hosts both the review summary AND the guest-details form; checkout/payment is a final action invoked from Step 3 via the dual-CTA pattern. No schema, API, or locked-pipeline change.

- **Phase 16A - WhatsApp CTA prefills reverted to plain human sentences (2026-05-23, [apps#54](https://github.com/Staleen/oraya-web/pull/54)).** The earlier structured-marker scheme (`#ORAYA_REF:<ref>` / `#ORAYA_CHANGE:<ref>`) is withdrawn. `bookingWhatsAppPrefill(ref)` in [lib/booking-trust-messaging.ts](../../lib/booking-trust-messaging.ts) now returns `"Check my booking <ref>"`; `bookingWhatsAppChangePrefill(ref)` returns `"Help with my booking <ref>"`. WhatChimp triggers on those substrings (the only platform capability available on the production tenant - see [KNOWN_BUGS.md](KNOWN_BUGS.md) #7). The 8-char reference inside the sentence is the public guest-facing support code from [lib/booking-reference.ts](../../lib/booking-reference.ts) and carries no new disclosure risk. The two prior 2026-05-23 marker entries in [DECISIONS_LOG.md](DECISIONS_LOG.md) are now superseded by the 2026-06-03 entry that records this reversion.

- **Phase 16A/16B - Booking flow polish + pay-now / reserve action unification (2026-05-23, [apps#46](https://github.com/Staleen/oraya-web/pull/46) + [apps#48](https://github.com/Staleen/oraya-web/pull/48) + [apps#49](https://github.com/Staleen/oraya-web/pull/49) + [apps#50](https://github.com/Staleen/oraya-web/pull/50)).** Booking-flow polish, the unified "pay now" booking action, and clarified review-before-payment behavior for add-ons / special requests. Approval-based add-ons are explicitly reviewed and charged separately after Oraya confirms; the stay payment is not blocked by them. No schema, API, or locked-pipeline change.

- **Phase 16A - WhatChimp marker-routing correction (2026-05-23, [apps#52](https://github.com/Staleen/oraya-web/pull/52)).** Now superseded by [apps#54](https://github.com/Staleen/oraya-web/pull/54) above - kept in the log for traceability. The 2026-05-23 marker correction documented the WhatChimp inbound-text limitation and corrected the operator routing for the marker-based prefill; the subsequent reversion to plain human sentences eliminates the marker scheme entirely.

- **Phase 16B.4 - Credit Libanais readiness foundation + provider-schema compatibility (2026-05-22, [apps#44](https://github.com/Staleen/oraya-web/pull/44)).** Added the human-gated migration [sql/phase-16b4-credit-libanais-provider-compat.sql](../../sql/phase-16b4-credit-libanais-provider-compat.sql) so `bookings.payment_link_provider` can safely persist `credit_libanais` alongside the older backward-compatible `manual` / `whish` / `stripe` values. [lib/payments/credit-libanais.ts](../../lib/payments/credit-libanais.ts) models the bank-integration contract explicitly without faking checkout: merchant id, gateway URL, session-creation endpoint, auth/signing method, callback verification method, provider session id field, currency/settlement behavior, and sandbox/live mode. Remaining contract verification is Oraya-owned work using official CyberSource documentation, Business Center configuration, sandbox/test tooling, and internal architecture decisions; it does not wait for further bank confirmation. [lib/payments/runtime.ts](../../lib/payments/runtime.ts) separates guest-safe readiness from admin-safe readiness, and `/admin/settings` surfaces the non-secret provider status plus missing requirements via [app/api/payments/readiness/route.ts](../../app/api/payments/readiness/route.ts). No Butler/WhatsApp changes. No booking, pricing, overlap, or payment-success simulation changes.
- **Phase 16A - Butler website handoff date/auth persistence hardening (this commit).** `normalizeLeadInput` now accepts plain `check_in` / `check_out` aliases in addition to the normalized WhatChimp fields when persisting `whatsapp_leads`, and also falls back to the nested `raw_payload` object WhatChimp sends when the ISO stay dates are not duplicated at the top level. This closes the production gap where villa/name/guest count were mapped from top-level fields but `oraya_check_in` / `oraya_check_out` existed only inside `raw_payload`, leaving `normalized_check_in` / `normalized_check_out` null and causing `/api/butler/prefill` to return null dates. On the `/book` client, Butler date hydration is queued into the exact `dateRange` state the calendar uses and is re-applied after the auth gate resolves, so late villa resets or the guest/member gate cannot drop the decoded stay dates, and the visible calendar month now follows the hydrated `dateRange` so a December handoff opens on December rather than staying anchored to the current month. The `/book` auth gate also preserves the full current booking URL in its member sign-in redirect, so the signed handoff token survives the login round-trip instead of depending on session timing. No schema changes. No locked API behavior changes.
- **Phase 16B - admin payment settings + Step 3 decision foundation (this commit).** `/admin/settings` now manages the guest-facing payment mode (`request_only`, `manual_payment`, `online_payment`, `hybrid`), deposit minimum percentage, full/custom deposit availability, manual payment rails, guest payment instructions, bank-transfer public details, provider display name, and the guest-visible online-payment enabled flag through the existing `settings` table. `/book` Step 3 now presents two clear Reserve paths: `Pay now and reserve` and `Submit booking request and pay later`. The pay-now path still uses hosted-checkout amount validation and the existing `/api/payments/checkout` route, but it is blocked in the UI whenever the configured provider is not truly ready. The pay-later path keeps booking-first behavior, records payment preferences in the booking request context, and makes it explicit that no charge is collected on the website until Oraya confirms the stay. No schema change. No locked booking-creation behavior change.

- **Phase 16A - WhatsApp -> website identity continuity (2026-05-18 merge, PR #27).** `/api/bookings` POST now accepts an optional `butler_prefill_token` in the request body. After a successful insert, the locked route best-effort verifies the token (`lib/butler/prefill-token.ts`) and updates `whatsapp_leads.linked_booking_id` with a `.is("linked_booking_id", null)` race guard so an existing linkage is never overwritten. Verification failure, expired token, missing lead, conflicting linkage, and Supabase errors all log a server-side warning and return early - they **never** block booking creation. `/book` reads the token from sessionStorage at submit time, sends it in the booking POST body, and clears the token on success. Closes the lead -> booking provenance loop without changing locked-pipeline behavior on failure. See [DECISIONS_LOG.md](DECISIONS_LOG.md) - 2026-05-18 entry "WhatsApp lead -> booking provenance linkage in `/api/bookings` POST".

- **Phase 16A - Butler prefill villa alias normalization (2026-05-18 merge, PR #26).** [app/book/page.tsx](../../app/book/page.tsx) `normalizeVillaFromSearchParam` now folds the operator-facing aliases `Byblos`, `Byblos/Jbeil`, `Jbeil` -> `Villa Byblos` and `Mechmech`, `Mechmech/Annaya`, `Annaya` -> `Villa Mechmech` when normalizing both `?villa=` and Butler-prefilled villa values. Aliases are case-insensitive. Unknown villa labels pass through unchanged so the canonical villa selection check still rejects them.

- **Phase 16A - Butler continuation auto-advance readiness gate (2026-05-18 merge, PR #25).** The non-instant auto-advance from step 1 -> 2 on `/book` is now gated on (a) `butlerPrefillReady` (the `?h=...` handoff hydration has settled, success or failure) and (b) `availabilityReadyForSelection` (the availability fetch for the prefilled villa+check-in has settled and is not loading). Prevents auto-advance from firing before prefill or availability resolves and causing a flash-of-mismatched-step on slower connections.

- **Phase 16A.2.j - WhatsApp -> website prefill handoff (prior commit).** Added [lib/butler/prefill-token.ts](../../lib/butler/prefill-token.ts) and [app/api/butler/prefill/route.ts](../../app/api/butler/prefill/route.ts) for a short-lived opaque handoff token signed with `BUTLER_PREFILL_SECRET`. POST `/api/butler/lead` now attempts to return an additive `prefill_url` after successful lead insert, but lead capture remains business-critical and **does not fail** if `BUTLER_PREFILL_SECRET` is missing - the URL is omitted instead. [app/book/page.tsx](../../app/book/page.tsx) now hydrates safe fields from `/api/butler/prefill?h=...` and strips `h` from the URL after success or failure. No schema changes. No locked-API touches. No raw booking data in the public URL. Normalized lead dates remain the only dates eligible for website prefill; raw WhatsApp text is never used for `/book` hydration.

- **Phase 16A.2.e - WhatsApp lead persistence + admin lead dashboard (this commit).** Added a new `whatsapp_leads` Supabase table (schema in [/sql/phase-16a2e-whatsapp-leads.sql](../../sql/phase-16a2e-whatsapp-leads.sql)) and the surfaces that read and write it:
  - `POST /api/butler/lead` ([app/api/butler/lead/route.ts](../../app/api/butler/lead/route.ts)) - first Butler write endpoint. Accepts the flexible WhatChimp payload (canonical `oraya_*` keys + short aliases), normalizes it via [lib/butler/leads.ts](../../lib/butler/leads.ts) `normalizeLeadInput`, inserts one row, returns `{ ok: true, lead_id, message }`. Reuses the existing 2026-05-12 Butler auth contract via `requireButlerAuth` (503 if env unset, 401 if header missing/wrong). Raw Supabase / driver errors collapse to a safe `{ ok: false, error: "server_error" }` 500 - never echoed.
  - `GET /api/admin/leads` ([app/api/admin/leads/route.ts](../../app/api/admin/leads/route.ts)) - admin list with optional `follow_up_status` / `request_type` / `villa` filters. `raw_payload` intentionally not returned in the list shape.
  - `PATCH /api/admin/leads/[id]` ([app/api/admin/leads/[id]/route.ts](../../app/api/admin/leads/%5Bid%5D/route.ts)) - admin update for `follow_up_status` (allow-listed: `new`/`contacted`/`needs_action`/`converted`/`lost`/`spam`), `labels`, `admin_notes`, `linked_booking_id`. Identity fields (`source`/`phone`/`name`/dates/`raw_payload`) intentionally not mutable from PATCH in v1.
  - `/admin/leads` ([app/admin/leads/page.tsx](../../app/admin/leads/page.tsx)) - admin dashboard page. Status-filter bar, status badges + inline status change, `wa.me` link for the phone, inline `admin_notes` edit, empty state. Matches the existing inline-style admin visual convention. A new "Leads" link was added to the admin top-nav in [components/admin/AdminChrome.tsx](../../components/admin/AdminChrome.tsx) - the minimum non-invasive change needed to make the page discoverable.
  - RLS posture for `whatsapp_leads`: **enabled with NO policies**. Service role bypasses RLS so both the Butler ingest and admin routes (both server-only via `SUPABASE_SERVICE_ROLE_KEY`) can read/write. Any future anon/authenticated client query is denied by default. Stricter than the repo's existing operational tables (e.g. `booking_action_tokens` runs RLS off) and the choice is documented in the SQL file + [DECISIONS_LOG.md](DECISIONS_LOG.md) (2026-05-15 entry).
  - **No booking creation, no availability check, no DB writes outside `whatsapp_leads`, no email sends, no token issuance, no payment, no access details, no smart-lock, no new dependency.** Lead intake only - operators triage from `/admin/leads`. `BUTLER_WEBHOOK_SECRET` and `X-Butler-Secret` are reused unchanged; no new env var. Active sub-phase remains `flow-submit`; this is the operational backbone the future `flow-submit` will hand off to.
- **Phase 16A.2.d - Butler `availability` yes/no POST (this commit).** Added an **additive** `POST /api/butler/availability` handler alongside the existing 16A.1 GET. The new POST takes `{ villa, check_in, check_out, request_type?, event? }` and returns a safe `{ status: "available" | "unavailable" | "unclear", safe_message, ... }` shape WhatChimp can repeat to the guest. Wraps [lib/calendar/availability.ts](../../lib/calendar/availability.ts) `findAvailabilityConflict` - the same overlap logic the locked `/api/bookings` POST uses - so the Butler answer is consistent with the booking-creation answer. Extended [lib/butler/villa.ts](../../lib/butler/villa.ts) `resolveButlerVilla` to accept canonical names (`"Villa Byblos"`) in addition to slugs (`mechmech`/`byblos`); existing GET callers continue to work unchanged. Response shapes live in a new [lib/butler/availability-formatter.ts](../../lib/butler/availability-formatter.ts). Reuses the existing 2026-05-12 Butler auth contract via `requireButlerAuth` (503 if env unset, 401 if header missing/wrong). **No booking creation, no DB writes, no email, no token, no schema change, no new dependency.** Internal errors collapse to `status: "unclear"` - raw Supabase / driver messages are never echoed. Active sub-phase remains `flow-submit`; this is additional read-only Butler scaffolding.
- **Phase 16A.2.c - Butler `normalize-dates` read-only scaffolding (this commit).** Added [lib/butler/normalize-dates.ts](../../lib/butler/normalize-dates.ts) and [app/api/butler/normalize-dates/route.ts](../../app/api/butler/normalize-dates/route.ts) - a secret-guarded `POST` endpoint that turns natural-language date text from WhatChimp (`"this Saturday"`, `"June 10"`, `"10 June 2026"`, `"two nights"`, `"24may"`, ISO) into a structured `{ check_in, check_out, nights, human_readable, safe_message }` suggestion. Returns `status: "clear"` when both dates parse, `status: "unclear"` otherwise; the `safe_message` always asks the Butler to echo back for guest confirmation before any availability check. Reuses the existing 2026-05-12 Butler auth contract via [lib/butler/auth.ts](../../lib/butler/auth.ts) `requireButlerAuth` (503 if env unset, 401 if header missing/wrong). **Does not create bookings, check availability, write DB rows, send emails, or issue tokens.** This is additional read-only Butler scaffolding for 16A.2 - the `flow-submit` adapter itself is still outstanding. See [DECISIONS_LOG.md](DECISIONS_LOG.md) - 2026-05-14 entry.
- **Phase 16A.1.x - Butler Playbook + minor hardening (this commit).** Established [BUTLER_PLAYBOOK.md](BUTLER_PLAYBOOK.md) as the operational source-of-truth for the AI Butler (identity, conversation behavior, availability/pricing/add-on philosophy, knowledge boundary, event vs stay separation, deferred future-phase systems, forbidden AI behaviors, cross-references). Extracted the duplicated villa-slug map from the addons + availability routes into a shared [lib/butler/villa.ts](../../lib/butler/villa.ts) helper (`resolveButlerVilla` + `KNOWN_BUTLER_VILLAS`). [ARCHITECTURE.md](ARCHITECTURE.md) Butler flow section cross-references the playbook. See [DECISIONS_LOG.md](DECISIONS_LOG.md) - 2026-05-12 "Butler Playbook established as operational source-of-truth". No code behavior change beyond the surgical helper extraction; same 503/401/200 contract on every `/api/butler/*` route.
- **Phase 16A.1 - Read-only Butler API foundation (prior commit).** Shipped:
  - [lib/butler/auth.ts](../../lib/butler/auth.ts) - `requireButlerAuth` helper. Validates `X-Butler-Secret` against `BUTLER_WEBHOOK_SECRET` using `crypto.timingSafeEqual`. 503 on missing/empty env; 401 on missing/wrong header.
  - [app/api/butler/health/route.ts](../../app/api/butler/health/route.ts) - liveness + secret check; returns `{ ok: true, service: "oraya-butler", mode: "read-only" }`.
  - [app/api/butler/event-types/route.ts](../../app/api/butler/event-types/route.ts) - projects `CANONICAL_EVENT_TYPES` from [lib/event-types.ts](../../lib/event-types.ts) into `{ value, label, description }`.
  - [app/api/butler/addons/route.ts](../../app/api/butler/addons/route.ts) - villa+context filtered add-ons with `event_type` optional. Prices and currency intentionally omitted; operational internals never echoed.
  - [app/api/butler/availability/route.ts](../../app/api/butler/availability/route.ts) - thin wrapper over `getMergedAvailabilityRanges` + heated-pool carryover. Does not modify or call the locked `/api/bookings/availability` route.
  - [ARCHITECTURE.md](ARCHITECTURE.md) - API surface table updated; new "Butler flow (Phase 16A.1 - read-only)" section added.
  - [ENVIRONMENT_MAP.md](ENVIRONMENT_MAP.md) - `BUTLER_WEBHOOK_SECRET` flipped from "reserved/not consumed" to "consumed by /api/butler/* (live)".
  - No locked-API touches. No schema changes. No new dependencies. `npx tsc --noEmit` clean. `npm run build` clean.
- **Phase 16A.0 - Architecture freeze ([apps#10](https://github.com/Staleen/oraya-web/pull/10)).** Locked namespace `/api/butler/*`, secret name `BUTLER_WEBHOOK_SECRET`, source-of-truth boundary, implementation order. Documented in [DECISIONS_LOG.md](DECISIONS_LOG.md) (2026-05-12).
- **Phase 16A audit (2026-05-11).** Read-only architecture audit; conclusions recorded in the 2026-05-12 DECISIONS_LOG entry.
- **AI Project Bootstrap (prior phase).** `/docs/system/` is the durable AI source of truth.

## Open issues to be aware of right now

- **WhatChimp remains an operational dependency outside this repo.** The exported flow must stay aligned with the backend contract: route on the `"Check my booking"` / `"Help with my booking"` substrings the website CTAs emit; call `POST /api/butler/identify` for identity resolution; call `POST /api/butler/confirmed-guest-info` for identity-established confirmed guests; capture and use `prefill_url` from `POST /api/butler/lead` when present. If WhatChimp drifts (e.g. back to a static `/book` link, or routing on the withdrawn `#ORAYA_REF:` / `#ORAYA_CHANGE:` markers), guests lose the secure continuation benefit even though the backend is healthy.

- **Natural-intake operator wiring (batch 2) — technical gate passed 2026-06-05, production flow migration still pending.** WhatChimp confirmed able to call `POST /api/butler/normalize-stay-intent` and map nested `extracted.*` response fields — architecture validated, endpoint reachable, nested field mapping verified. The production stay-booking flow has not yet been migrated: the `oraya_stay_text` custom field, new trigger, capture node, HTTP API call, response-driven branches, and retirement of the old four-step intake nodes are still pending operator action in the production tenant.

- **Confirmed-guest support wiring is the active operator-side closeout.** The `/api/butler/confirmed-guest-info` endpoint, the operator-managed `butler_checkin_guidance` settings row, the disclosure boundary, and the explicit `location_access_note` are all live. The WhatChimp flow that calls this endpoint for identity-established confirmed guests still needs to be wired in the production tenant.

- **Generic AI must not answer booking-sensitive confirmed-guest questions outside the dedicated boundary.** Villa, stay dates, signed booking-view URL, check-in guidance, and location-access policy belong to `/api/butler/confirmed-guest-info`. The orchestrator is the single source of truth per turn; AI Training / Bot Reply must never paraphrase or invent any of these fields.

- **AI wrong-domain response risk.** AI Training (WhatChimp or any external assistant outside this repo) has previously returned `www.oraya.com.lb` and similar hosts. The canonical Oraya origin is **`https://stayoraya.com`** and only `https://stayoraya.com`. This is **not** a domain migration; it is a wrong-domain bug whenever it appears. See [KNOWN_BUGS.md](KNOWN_BUGS.md) and the canonical-domain section in [BUTLER_PLAYBOOK.md](BUTLER_PLAYBOOK.md).

- **Booking reference is a public support code, not an access PIN.** The 8-character uppercased prefix of `bookings.id` that appears on `/booking/view/[token]` and in emails is intentionally a guest-facing support reference only. There is **no access PIN, smart-lock PIN, or gate code** in Phase 16A or Phase 16B. Access credential issuance is Phase 16D (smart lock). The Butler must never present the booking reference as an access PIN or imply it grants entry.

- **BUTLER_PREFILL_SECRET must be set in Vercel for website handoff.** Without it, POST /api/butler/lead still succeeds but omits `prefill_url`, and `/api/butler/prefill` cannot verify tokens. This is an intentional business-continuity trade-off for lead capture, but production needs the env set before the handoff can be relied on. The same secret is consumed by `/api/bookings` POST to verify `butler_prefill_token` for identity-continuity linkage - failure to verify is non-blocking; the booking still goes through.

- **Missing `RESEND_API_KEY` is a stealth failure.** Without Resend wired, no transactional email goes out and no error surfaces. See [KNOWN_BUGS.md](KNOWN_BUGS.md) #2.
- **Missing `NEXT_PUBLIC_SITE_URL` on preview links to production.** Preview-environment Butler messages and hosted-checkout return URLs would point at live data. Set `NEXT_PUBLIC_SITE_URL` on Vercel Preview. See [KNOWN_BUGS.md](KNOWN_BUGS.md) #3.
- **`BUTLER_WEBHOOK_SECRET` Vercel posture.** Production and Preview need the value set (Sensitive) before WhatChimp can call any `/api/butler/*` route in those environments. See [KNOWN_BUGS.md](KNOWN_BUGS.md) #4.
- **DECISIONS_LOG header-name example drift.** The 2026-05-12 DECISIONS_LOG entry used `X-Butler-Auth` as an illustrative header name; the actual implementation in 16A.1 uses `X-Butler-Secret`. Architecturally identical; only the header name differs.
- **WhatChimp inbound-text platform limitation.** The production WhatChimp tenant does not expose inbound message text as a Condition system field or HTTP API body variable - only first name, last name, label, email, phone number, chat ID are available. The backend's `message_text` extractor is forward-compatible code for channels that DO expose inbound text. See [KNOWN_BUGS.md](KNOWN_BUGS.md) #7.
- **Declined-flow sandbox validation is still open.** The attempted decline-style sandbox card authorized successfully, so Oraya must use official CyberSource test documentation and available sandbox/Business Center capabilities to produce and validate a terminal decline before marking decline handling complete. Further NetCommerce contact is not required.
- **Payment execution is not production-ready.** Durable idempotency, monotonic attempt-state safety, the reconciliation core, manual-first refunds, and the `payments_live_enabled` switch are merged. The webhook receiver is still a temporary raw-body HMAC mechanism. Production JWT request authentication, response MLE, webhook JWE decryption, timestamped digital-signature verification, replay protection, webhook subscription/setup, credentials/env, human-run SQL migrations, declined-card validation, and deliberate enablement remain required. The placeholder webhook configuration must not be enabled or subscribed in production. Settlement reconciliation, payment email validation, WhatsApp payment-status replies, and Instant Book execution are also not part of the live path; refunds automation is deferred by policy.
- **Payment-provider envs are required for the hosted Reserve payment path.** Missing or incomplete provider configuration means Step 3 creates the booking row and sends the guest to `/booking/view/[token]?payment=pending`; provider-readiness and setup-failure detail stay on admin/server surfaces until Oraya follows up manually.

## Out of scope this phase

- No schema changes without explicit approval in the task prompt. If a new `butler_submissions` table is chosen over the jsonb-enrichment path, that decision goes through a separate approval gate.
- No production Credit Libanais / NetCommerce rollout until declined-flow validation succeeds through official CyberSource test documentation or available sandbox tooling, production credentials are configured and validated internally, production JWT request authentication and response MLE are implemented, webhooks have JWE decryption plus timestamped digital-signature verification and replay protection, provider webhook subscription/setup is complete, Vercel Production env is configured deliberately, the human-run SQL migrations are applied, and production enablement is approved. Do not enable or subscribe the current HMAC placeholder. Browser success/cancel returns remain informational only; the `payments_live_enabled` switch is necessary but not sufficient. No further NetCommerce answer or approval is required.
- No saved-card/tokenization launch in the current NetCommerce increment. Saved cards are now a later Phase 16B workstream, but CyberSource TMS/provider tokenization, explicit consent, member instrument management, revoke controls, and the customer-initiated versus merchant-initiated policy must be designed, reviewed, and approved before activation.
- No refunds automation. Refunds are manual-first by explicit decision (Plan 4 Phase 1, 2026-07-25): executed by hand in the NetCommerce Business Center and recorded via "Record manual refund" ([REFUND_RUNBOOK.md](REFUND_RUNBOOK.md)). Automated provider-side refunds are a separate, later plan.
- No Instant Book payment execution. The instant-book UI exists but executes through the same hosted-checkout placeholder today; live instant execution remains later 16B work.
- No smart-lock PIN issuance or access-code delivery. Phase 16D.
- No member -> phone linkage. Every Butler-originated booking is the guest path. A future phase ships the verification flow.
- No AI prompt engineering in this repo. AI Training, Bot Reply, Labels, and Custom Fields live in WhatChimp.
- No `/api/bookings` POST behavior change. The booking pipeline (overlap, pricing, addon audit, email triggers, view-token issuance) remains the authoritative source of truth.
- No locked-API touches. `/api/bookings*`, `/api/admin/*`, `/api/calendar/*`, `/api/cron/*`, the email senders, the auth/token systems, and existing schema remain off-limits.
- No widening of `/api/settings` allowlist to satisfy WhatChimp. Butler reads belong under `/api/butler/*`.
- No `NEXT_PUBLIC_BUTLER_*` env vars. Server-only.
- No re-introduction of the withdrawn `#ORAYA_REF:` / `#ORAYA_CHANGE:` marker prefill scheme without an explicit superseding decision-log entry.

## Next recommended steps

In order:

1. **Preserve and close the active NetCommerce increment:** review/merge PR #109 (Phase 16B-2B1 JWT/JWS + payment request/response MLE) once its checks and scope are accepted. Do not duplicate that work on another branch.
2. **16B-F1 - ledger and request foundation:** from updated `master` on a fresh branch, design and implement additive `payment_requests`, `payment_transactions`, permissions/audit rules, and booking summary projections. No live provider activation.
3. **16B-F2 - cash first:** implement end-to-end cash receipt, partial/multiple payments, receipt output, reversal/adjustment, and USD/LBP reconciliation in operations; reuse it for truthful manual bank/Whish/OMT/Western Union/provisional Suyool records.
4. **16B-F3 - standalone links:** create the safe `/pay/<opaque-token>` flow for callers and WhatsApp contacts without bookings, with copy/send, expiry, cancel, reminder, allowed-method selection, and optional later booking association.
5. **NetCommerce production completion:** preserve all merged/PR #109 safety controls, replace the temporary webhook receiver with JWE decryption plus timestamped signature and replay protection, complete subscription/setup, declined-flow and settlement validation, credentials/migrations, payment email lifecycle, and deliberate activation.
6. **Additional methods:** enable Apple Pay within the approved hosted provider, then integrate Whish, OMT/WU, and the confirmed "Sunbook Pay" provider independently after merchant/API contracts and reconciliation behavior are verified.
7. **Member saved cards:** implement provider-tokenized, explicit-consent instruments and revoke controls only after the one-time-card path and legal/security operating policy are ready.
8. **Separate non-payment work:** WhatChimp confirmed-guest wiring and any future `POST /api/butler/flow-submit` remain independently scoped and must not bypass the payment or locked booking authorities.
