# Known Bugs & Open Issues

**Updated:** 2026-07-17

Living list of bugs, gaps, and operational pitfalls that are **known** but **not yet fixed** (or accepted as a permanent trade-off). New AI sessions: read this before assuming production is in a clean state.

**Severity scale:**

- **🔴 Critical** — production-impacting, data-loss risk, security risk, or breaks core user journey. Fix immediately.
- **🟠 High** — degrades user trust, enables silent failures, or creates operational risk. Fix this phase.
- **🟡 Medium** — observable rough edge, technical debt, or footgun for future agents. Fix opportunistically.
- **🟢 Low** — cosmetic or doc-only.

**Format:**

```
### #N — Short title
- **Severity:**
- **Area:**
- **Description:**
- **Status:** open | in-progress | won't-fix | wontfix-with-rationale
- **Recommended fix path:**
- **Discovered:** YYYY-MM-DD (source)
```

---

### #1 — `RESEND_FROM_EMAIL` is documented and slotted in `.env.example` but consumed by zero code paths

- **Severity:** 🟡 Medium
- **Area:** Email / configuration hygiene
- **Description:** `RESEND_FROM_EMAIL` previously appeared in [/.env.example](../../.env.example) and is referenced in [/README.md](../../README.md), but no code read it. Every `lib/send-*-email.ts` defines a hardcoded constant `FROM_EMAIL = "Oraya Reservations <bookings@stayoraya.com>"` and uses that in the Resend `from:` field. Setting the env var did nothing — silently. Future operators could have set it expecting an override and gotten confused.
- **Status:** **closed (resolved 2026-05-09)** — Option B taken. `RESEND_FROM_EMAIL` removed from [/.env.example](../../.env.example) and from the [ENVIRONMENT_MAP.md](ENVIRONMENT_MAP.md) inventory. From-address remains hardcoded in `lib/send-*-email.ts`. See [DECISIONS_LOG.md](DECISIONS_LOG.md) — 2026-05-09 entry "`RESEND_FROM_EMAIL` removed from env contract; from-address stays hardcoded". Future configurability is a separate approved implementation task. The remaining mention in [/README.md](../../README.md) is informational ("currently hardcoded… unless you later wire `RESEND_FROM_EMAIL`") and accurate; can be tightened in a future README pass.
- **Recommended fix path:** n/a — resolved.
- **Discovered:** 2026-05-09 ([ENVIRONMENT_MAP.md](ENVIRONMENT_MAP.md) audit)
- **Resolved:** 2026-05-09

---

### #2 — Missing `RESEND_API_KEY` is a silent failure (no user-facing alarm)

- **Severity:** 🟠 High
- **Area:** Email / observability
- **Description:** Every `lib/send-*-email.ts` checks for `RESEND_API_KEY` and **returns silently** (with a `console.warn`) if it is missing. Bookings still write to Postgres; no email goes out; no error surfaces to guest, admin, or any monitoring system. A misconfigured Vercel env (or a key rotated and not re-added) would result in zero confirmations being delivered while bookings continue to land — invisible until a guest complains.
- **Status:** RESOLVED 2026-07-24 (Plan 3 Phase 4 PR) — both recommended paths 1 and 2 shipped: `GET /api/health` (`lib/health.ts` pure decision + `app/api/health/route.ts`, force-dynamic) returns 200 `{ok:true}` when all required production keys are present and 503 listing the missing key NAMES (never values) otherwise — checks `RESEND_API_KEY`, `ADMIN_SECRET`, `ADMIN_RECOVERY_EMAIL`, `NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_ANON_KEY`, `SUPABASE_SERVICE_ROLE_KEY`. All seven silent `console.warn` sender sites now go through `lib/email-config.ts` `reportMissingResendKey()`, which in production emits a structured `console.error` with the stable grep-able tag `[email-config-missing]` (warn outside production). Optional human follow-up: point an uptime monitor (Vercel checks / UptimeRobot) at `/api/health`. Tests: `lib/health.test.mts` (6).
- **Recommended fix path:**
  1. Add a startup or healthcheck assertion: e.g. a `/api/health` endpoint that returns 503 if `RESEND_API_KEY` is unset in production.
  2. Or, narrower: in each `lib/send-*-email.ts`, when missing in production, write a structured log line that the existing log-aggregation surface picks up.
  3. Or, broader: track delivery in `bookings.email_status` and surface "delivery failed" in the admin dashboard.
- **Discovered:** 2026-05-09 ([ENVIRONMENT_MAP.md](ENVIRONMENT_MAP.md) audit)

---

### #3 — `NEXT_PUBLIC_SITE_URL` unset on preview links to production

- **Severity:** 🟡 Medium (high if preview emails are sent to real guests)
- **Area:** Email / preview parity
- **Description:** All transactional email senders fall back to `SITE_URL` from [/lib/brand.ts](../../lib/brand.ts) (`https://stayoraya.com`) when `NEXT_PUBLIC_SITE_URL` is unset. A preview deployment that sends a test email will embed links pointing at **production**, not at the preview. Guests testing on preview would land on real production data. No code error — purely an operational footgun.
- **Status:** open
- **Recommended fix path:**
  1. Set `NEXT_PUBLIC_SITE_URL` to the per-deployment Vercel URL on the **Preview** environment in Vercel's env panel. (Production keeps `https://stayoraya.com`.)
  2. Document in [/.env.example](../../.env.example) (already done in the 2026-05-09 audit pass) and in onboarding.
  3. Optional code-side hardening: if `NODE_ENV !== "production"` and `NEXT_PUBLIC_SITE_URL` is unset, throw at module load — no silent fallback to production URLs from non-prod environments.
- **Discovered:** 2026-05-09 ([ENVIRONMENT_MAP.md](ENVIRONMENT_MAP.md) audit)

---

### #4 — Vercel env values not yet manually populated

- **Severity:** 🟠 High (until done)
- **Area:** Deployment readiness
- **Description:** [ENVIRONMENT_MAP.md](ENVIRONMENT_MAP.md) and [/.env.example](../../.env.example) are accurate, but Vercel's Project → Settings → Environment Variables has not been re-verified against the audit. If a variable is missing or stale in Vercel, a redeploy could hit it with no obvious signal.
- **Status:** open — human action item.
- **Recommended fix path:** follow the "Recommended next steps" section of [ENVIRONMENT_MAP.md](ENVIRONMENT_MAP.md) — set/verify each variable per environment scope (Production / Preview / Development), mark sensitive ones, then redeploy and check production logs for env-related throws.
- **Discovered:** 2026-05-09

---

### #5 — `git push origin master` snippet in `/CLAUDE.md` is legacy advice

- **Severity:** 🟢 Low (doc-only) — but a real footgun if an agent follows it literally
- **Area:** Documentation / agent workflow
- **Description:** [/CLAUDE.md](../../CLAUDE.md) contains an "Auto-backup rule (MANDATORY)" snippet `git add -A && git commit -m "…" && git push origin master`. Current operational rule is to push to feature/worktree branches and open PRs (see [AGENT_RULES.md](AGENT_RULES.md) rule 5). An agent that obeys CLAUDE.md literally would push directly to `master`, bypassing review.
- **Status:** open
- **Recommended fix path:** edit [/CLAUDE.md](../../CLAUDE.md) to replace the `master` push with feature-branch + `gh pr create`. Or add a one-line note pointing to [AGENT_RULES.md](AGENT_RULES.md) as the authoritative workflow. Defer until next doc-cleanup pass to avoid scope creep on the bootstrap phase.
- **Discovered:** 2026-05-09 (during AI bootstrap doc set)

---

### #6 — Website-CTA WhatsApp continuity: booking reference inside trigger message is lost; bot redundantly asks for it

- **Severity:** 🟠 High — degrades premium hospitality UX on the primary returning-guest entry point.
- **Area:** WhatsApp Butler / `/api/butler/identify` / WhatChimp Guest Identification v2 flow
- **Description:** the website "Continue on WhatsApp" CTA opens WhatsApp pre-filled with a context line like `"Hello Oraya — booking reference A0B8CECB"`. The Guest Identification v2 trigger correctly fires on the `"booking reference"` substring, but the 8-character hex value `A0B8CECB` is never captured into a custom field — WhatChimp's Condition / save-to-custom-field primitives can route on substring matches but cannot run a regex capture to lift a token out of the trigger message body. The flow therefore reaches the HTTP API call at Node 7 (`/api/butler/identify`) with `booking_reference` empty. For a fresh visitor with no prior `whatsapp_leads.linked_booking_id`, the orchestrator's priority chain falls through to `ask_for_booking_reference`, and the bot asks the guest to type the reference they had just provided in the trigger message. Naive recovery (passing the entire trigger message into the existing `booking_reference` field and letting `normalizeBookingReference` strip non-hex chars) is unsafe — the string `"Hello Oraya — booking reference A0B8CECB"` contains valid hex letters scattered through "Hello/Oraya/booking/reference" and would be silently mis-extracted as `EAABEFEE`, producing a confidently-wrong "I couldn't find that booking" response.
- **Status:** **closed (resolved 2026-05-23)** — `/api/butler/identify` now accepts an optional `message_text` body field. When `booking_reference` is absent and `message_text` is present, the route extracts the first word-boundary-anchored 8-character hex token via [lib/butler/extract-booking-reference.ts](../../lib/butler/extract-booking-reference.ts) (regex `/\b[0-9A-Fa-f]{8}\b/`) and forwards it as the `booking_reference` to the orchestrator unchanged. Existing callers that pass `booking_reference` explicitly continue to win; `message_text` never overrides an explicit reference. No naive hex stripping is performed anywhere on this path. No schema changes; no locked-API touches; no auth changes; no payment-file touches.
- **Recommended fix path:** n/a — resolved.
- **Discovered:** 2026-05-23 (live production traffic + WhatChimp flow export `whatsapp-bot_1858233_20260523090930.txt` audit).
- **Resolved:** 2026-05-23
- **Follow-up (2026-05-23):** the BACKEND closure of #6 is correct, but live testing then verified that WhatChimp specifically cannot deliver inbound-message text into the HTTP API body — see new entry #7 below. The production operator path therefore uses an explicit booking-reference input step triggered by the website CTA markers (`#ORAYA_REF:` / `#ORAYA_CHANGE:`); `message_text` is forward-compatible code that activates the moment a future channel — or a future WhatChimp version — exposes inbound text. See [BUTLER_PLAYBOOK.md](BUTLER_PLAYBOOK.md) "Website CTA marker routing" for the current operator routing.
- **Follow-up (2026-07-09) — production wiring consolidated; trigger-conflict risk removed.** The final Phase 16A WhatChimp closeout **deleted the duplicate standalone "website booking" / "Check my booking" / "Help with my booking" mini-flows** and consolidated all booking-support triggers into the single identify-first **Guest Identification v2** flow (trigger → `POST /api/butler/identify` immediately; unknown subscriber → ask 8-char reference → `oraya_booking_reference` → identify again; proof needed → email/full name → `oraya_identity_proof` → identify again; final booking-sensitive reply always `#oraya_identity_safe_message#`). This removes the trigger-matching conflict where a duplicate mini-flow could steal a booking-support message from the correct flow. The redundant-reference-ask itself is accepted as the designed fallback for unknown subscribers on a tenant that cannot lift the reference from the trigger text (#7). Builder method, safe trigger strategy, and release checklist are preserved in [BUTLER_PLAYBOOK.md](BUTLER_PLAYBOOK.md) "Phase 16A WhatChimp production builder (LOCKED 2026-07-09)"; see [DECISIONS_LOG.md](DECISIONS_LOG.md) 2026-07-09.

---

### #7 — WhatChimp does not expose inbound message text as a usable variable; auto-extract paths cannot run on this platform today

- **Severity:** 🟡 Medium — platform constraint, not a code defect. Documented so future agents do not wire against a capability that does not exist on the current production tenant.
- **Area:** WhatsApp Butler / WhatChimp variable model / operator-side flow design
- **Description:** the WhatChimp UI variable picker — the canonical way to reference dynamic values inside Condition nodes, save-to-custom-field steps, and HTTP API request bodies — exposes only **six** system fields on the production Oraya tenant: first name, last name, label, email, phone number, chat ID. There is no "last user message" / "last incoming text" / "trigger message" / "user input" variable. There is no Condition operator that runs against the message body. The HTTP API node body cannot interpolate the inbound message text. The implication for any flow design pattern that relies on lifting the user's text into a custom field or into an outbound request body is that the pattern is not achievable on this tenant. This blocks the auto-extract path that PR #47 wired on `/api/butler/identify` from being reached on the production WhatChimp tenant, even though the backend code is correct and forward-compatible with channels that DO expose inbound text.
- **Status:** **open (operator-side platform constraint; backend remains forward-compat)**. No code action required. The production operator routing for the website CTA markers uses an explicit reference-input step downstream of the marker trigger, captures the user's typed reply into `oraya_booking_reference`, and merges back into the existing identity flow. See [BUTLER_PLAYBOOK.md](BUTLER_PLAYBOOK.md) "Website CTA marker routing" → "Verified WhatChimp platform limitation" + "Operator manual steps required in WhatChimp" for the documented flow.
- **Recommended fix path:** none required for the backend. Operator-side: if WhatChimp later exposes an inbound-text variable, the operator can add `"message_text": "<the-new-variable>"` to HTTP API 7219's body and the existing extractor activates automatically — no Oraya code change. Independently: if Oraya adds a non-WhatChimp channel (Telegram, Messenger, direct WhatsApp Cloud API) that does expose inbound text, the same backend already supports it.
- **Discovered:** 2026-05-23 (live operator UI verification).

---

### #8 — AI / WhatChimp wrong-domain response risk: `www.oraya.com.lb` is not the Oraya web origin

- **Severity:** 🟠 High — guests routed at a non-existent domain lose access to the booking flow, the booking-view page, and every email CTA target.
- **Area:** AI Training / WhatChimp Bot Reply / generic AI assistants outside this repo / human ops copy
- **Description:** the canonical Oraya web origin is `https://stayoraya.com` and only `https://stayoraya.com`. This is the host every transactional email helper builds links from (`lib/brand.ts` `SITE_URL` fallback), every `/legal/*` page is served from, every `/booking/view/[token]` link points at, and every `/api/payments/checkout` return URL lands at. Despite this, generic AI assistants outside Oraya (including external WhatChimp configurations and untrained chat surfaces) have on occasion produced replies that name `www.oraya.com.lb` (or `oraya.com.lb`, or `oraya.com`) as if those were Oraya web properties. They are not. There is no LB-TLD Oraya web property today. This is **not** a domain migration — it is a wrong-domain bug whenever it appears.
- **Status:** open — operator-side AI Training discipline + documentation hardening.
- **Recommended fix path:**
  1. Make the canonical-origin invariant explicit in all AI-facing docs (PROJECT_STATE.md, BUTLER_PLAYBOOK.md, DECISIONS_LOG.md — landed 2026-06-03).
  2. Ensure WhatChimp Bot Reply / AI Training templates never reference any host other than `https://stayoraya.com`.
  3. If a guest reports they were directed at `oraya.com.lb` from any AI surface, treat it as a high-priority operator-side configuration bug.
  4. Optional: server-side hardening — a future task could add a 301 redirect from `oraya.com.lb` to `stayoraya.com` if the LB TLD is ever registered defensively. Not in scope today.
- **Discovered:** 2026-06-03 (reconciliation pass — risk surfaced from external observations of generic AI behavior, not from production traffic against Oraya's own surfaces).

---

### #9 — NetCommerce / CyberSource declined-card sandbox validation still needs provider vector

- **Severity:** 🟡 Medium — payment QA / open validation item, not a production bug.
- **Area:** Phase 16B / NetCommerce / CyberSource Unified Checkout sandbox validation
- **Description:** PR #64 passed the approved-card sandbox path and NetCommerce confirmed successful testing before the implementation merged on 2026-07-02. Normal behavior leaves `bookings.status` as `PENDING`; the temporary Preview QA auto-confirm exception was removed after external testing. Declined-card validation is still incomplete because the attempted decline-style sandbox card authorized. Oraya needs the official NetCommerce/CyberSource declined-card vector or decline trigger before declaring decline handling validated.
- **Status:** open — waiting on NetCommerce/CyberSource test vector or trigger.
- **Recommended fix path:**
  1. Ask NetCommerce/CyberSource for the official declined-card sandbox vector or configured decline trigger.
  2. Re-run the declined-card browser flow on a dedicated sandbox Preview.
  3. Confirm the declined attempt does not mark payment paid and remains retryable.
  4. Keep production payment disabled until production credentials, webhook/MLE reconciliation, production env setup, and explicit production enablement are complete.
- **Discovered:** 2026-06-17 (PR #64 Preview sandbox validation).

---

### #10 — WhatChimp natural-intake v6: merge-node round-trip survival and response-mapping overwrite behavior are unverified platform semantics

- **✅ RESOLVED — 2026-07-09 (Phase 16A v6 LOCKED, operator-approved).** PR #69 merged (`92c144197d72ef5bcc3bb76f9615c3ea4481339c`) and production-deployed; the backend extractor lead-in fix (`0ad7dbf`) is live on `www.stayoraya.com`. David completed the mandatory Stay Intent / Refine response mappings (each date on two rows — final field AND `_text` mirror) and passed live production WhatsApp verification on the normal API links: (1) full one-message intake `mechmech july 10 to july 15 for 4 people 2 bedrooms` → dated summary with both dates, villa, 4 guests, 2 bedrooms, secure link, ZERO questions; (2) missing-checkout `mechmech from july 10 …` → asked check-out, did NOT reuse the stale July 15, confirmed `2026-07-10 → 2026-07-12`; (3) repeated missing-checkout `mechmech july 10 …` → asked and confirmed correctly. Both merge-node round-trip survival AND the stale-date response-mapping overwrite are now proven in live production. The operator's post-mapping export is **not committed to the repo** — it remains on the WhatChimp platform (reviewed export: 192 nodes, observed SHA-256 `C80F325D…76E9`). **Remaining optional polish only (non-blocking):** a check-out-before-check-in reply (e.g. check-in July 10, check-out June 15) falls back with "couldn't read that" instead of an explicit "Check-out must be after check-in." message — deferred UX nicety, not required. Full record: DECISIONS_LOG 2026-07-09 "Phase 16A v6 structured-date intake LOCKED". The history below is preserved for provenance.
- **Severity:** ✅ Resolved (was 🟠 High until the test-bot round trip passes) — both risks can silently break the imported flow even though the artifact validates clean in the repo.
- **Area:** WhatsApp Butler / WhatChimp platform behavior / `Oraya_natural_intake_v6.txt`
- **Description:** WhatChimp behaviors that the v6 flow depends on cannot be verified from exports alone. (1) **Merge survival:** v6 uses multi-input merge nodes (guest gate, villa gate, shared escalation tail, shared large-group review). The live v4.3.3 production export proves multi-input nodes CAN survive export, but an earlier operator upload lost edges; whether a fresh import → save → re-export preserves every inbound arrow on this tenant is unproven. (2) **Mapping overwrite-on-missing:** stale-field safety requires that every normalization call overwrite `oraya_check_in`/`oraya_check_out`/`oraya_villa`/`oraya_guest_count`. The backend now guarantees string values via the additive `extracted_text.*` response object (literal `"null"` when missing), but the operator must rebind the 7466/8101 response mappings to `extracted_text.*`, and the write-always behavior still needs one live confirmation. (3) **HTTP-failure continuation:** whether the live runtime continues along an HTTP API node's output edge or halts when the call fails is untested (the graph has no status branching). Mitigations shipped in the artifact: the opening intake question shows the canonical `https://stayoraya.com/book` safety link BEFORE the first API node is reachable (validator `pre-api-safety-link` + simulator ordering invariant), and every terminal carries the continuation links — so the guest holds an actionable path in both the halt and continue cases. Checklist C11 probes all four TEST integrations with isolated forced failures. (4) A smaller unknown: rendering of the 9-option guest quick-reply question on the live WhatsApp channel.
- **Status:** open — resolved only by the human round-trip + live checklist in [artifacts/whatchimp/V6_ROUNDTRIP_CHECKLIST.md](../../artifacts/whatchimp/V6_ROUNDTRIP_CHECKLIST.md) (validator re-run: `node scripts/validate-whatchimp-flow.mjs <re-export> --strict-binding --bedroom-field-id 69114`).
- **Follow-up (2026-07-03) — loose question links confirmed and repaired:** the operator imported the earlier v6 build and observed loose/disconnected links around the guest-count, bedroom, bedroom-retry, villa, confirmation, and Looks-right/Edit steps (initial and Edit paths). Root cause: 9 generated edges wired question nodes **directly** into Condition nodes / User Input Flow wrappers — transitions absent from all 22 genuine WhatChimp exports surveyed (operator's v5.5 + 21 platform-named exports, 167 question nodes, zero exceptions; evidence table with hashes and per-file censuses in [artifacts/whatchimp/V6_TRANSITION_EVIDENCE.md](../../artifacts/whatchimp/V6_TRANSITION_EVIDENCE.md)). The generator now builds every such edge as `Question → acknowledgement Text → next` (the operator's own v5.5 device, `#491 → #492 "Got it." → #493`); the validator's new `question-transition` check errors on any direct Question → Condition / Question → User Input Flow edge; the simulator asserts node-level visit order on the repaired paths. The real bedroom field id `69114` (operator-created) is emitted directly by the generator, so the single canonical import file `Oraya_natural_intake_v6.txt` ships fully bound with no placeholder and no second binding step. Save compatibility is still **not** claimable from the repo — the import → save → close → reopen → export test (checklist A2/B) remains the gate.
- **Follow-up (2026-07-03) — authenticated round trip #1 executed: merge survival is now ANSWERED, and the answer is no. The current canonical candidate is NOT import-safe.** The operator ran the full sequence (import `Oraya_natural_intake_v6.txt` → save → close → reopen → export). Import, save, and reopen succeeded mechanically and all 118 nodes survived with every binding intact (incl. `69114`/`custom_69114`), but WhatChimp kept only the **first serialized connection per input socket** and silently removed the other **32 complete reciprocal edges** — all 16 multi-parent nodes were reduced to exactly one parent (the first-listed connection survived in 16/16 cases), creating 16 unintended terminals. Validator on the re-export: exit 1, 96 errors; simulator: 6/42. Broken business paths include every bedroom-capacity route, every escalation except the first date failure, the entire Edit path, and the date-recovery merges. Evidence + complete lost-edge table: [artifacts/whatchimp/roundtrips/ROUNDTRIP_1_FINDINGS.md](../../artifacts/whatchimp/roundtrips/ROUNDTRIP_1_FINDINGS.md) (re-export preserved byte-for-byte, SHA-256 `6ED2E190…7C89`); machine comparison: `node scripts/compare-whatchimp-roundtrip.mjs`. The validator's new `single-parent-contract` check now rejects any convergent import candidate (the current artifact fails it with exactly those 16 violations). **The previously documented fallback ("no-merge exploded rebuild of the affected tails") is quantified as infeasible in full: a complete single-parent unroll of the current behavior graph is exactly 492,864 nodes.** Two facts bound the solution space: (a) imported convergence does not survive save; (b) editor-drawn convergence does survive save/export (the operator's own v5.5 carries a 5-parent node `#440`, and the v4.3.3 production export carries multi-input nodes). The structural direction — partial tail cloning plus a bounded operator redraw checklist, versus an approved reduction of the gating behavior to fit a pure tree — is an operator decision, pending (see DECISIONS_LOG 2026-07-03).
- **Follow-up (2026-07-03) — Option A hybrid candidate shipped; the gate is now round trip #2 including the operator redraw.** The operator selected Option A (DECISIONS_LOG 2026-07-03 "Option A hybrid rebuild"), and the generator was rebuilt to it: every small linear tail is cloned branch-locally as single-parent (10 escalation tails, the Edit large-group review, and a complete Edit-handoff clone with its own Lead Submit nodes and terminals), while the 11 irreducible hub merges are declared in the profile's `importGraphContract.approvedHubMerges` and left convergent — after import the operator draws **exactly 18 connections** following the machine-generated [artifacts/whatchimp/V6_REDRAW_CHECKLIST.md](../../artifacts/whatchimp/V6_REDRAW_CHECKLIST.md). New canonical `Oraya_natural_intake_v6.txt`: 165 nodes, 182 output connections, SHA-256 `0066192D4487ED1AC8A95B00E22DEF8B754B1FB458E0025B178E8B125C913A39`; validator `--strict-binding` exit 0 (0 errors, 0 warnings — `single-parent-contract` now accepts only the declared hubs at exact inbound counts, so an un-repaired re-export is detected as hub-count drift); simulator 50/50 with all 14 terminals covered; tooling tests 30/30 incl. byte-for-byte generator reproducibility. The un-repaired import is safe by construction (first-listed hub edges = the happy path, opening-question safety link, no confirmation claims on any terminal). Merge survival of the **redrawn** edges is export-proven in principle (v5.5 `#440`, v4.3.3) but this candidate's status stays open until round trip #2 — import → 18 redraws → save → close → reopen → export → comparator PRESERVED exit 0 + validator exit 0 — passes on a fresh disposable bot.
- **Follow-up (2026-07-03) — round trip #2 HALTED: the editor refuses Condition → Condition connections; the hybrid redraw plan is not operator-executable.** The operator attempted redraw item #1 on a fresh disposable bot and WhatChimp rejected the connection with *"This will make an infinite loop. Place a button/list/section/interactive between these two nodes."* The machine audit of all 18 planned draws: **11 are Condition → Condition** (items #1–4, #6–8, #12, #14–16 — hubs `440`/`470`/`660`/`690` cannot receive their drawn edges at all); the other 7 type pairs (Text→Condition ×2, Condition→Text ×2, Text→User Input Flow ×2, HTTP API→Condition ×1) have never been proven drawable. Critically, the v5.5 `#440` 5-parent merge that had been cited as drawability evidence is itself Condition → Condition ×5 — export survival does not prove present-editor drawability. Evidence, full audit, and the quantified behavior-preserving rebuild direction (Condition-clone cascade: conditions are guest-invisible, so cloning the 7 convergent condition hubs per-parent adds 21 nodes and moves all merges onto interactive/Text nodes at the cost of 39 draws — feasible **only if** `Condition → User Input Flow` / `Text → User Input Flow` / `Condition → Text` prove drawable): [artifacts/whatchimp/roundtrips/ROUNDTRIP_2_FINDINGS.md](../../artifacts/whatchimp/roundtrips/ROUNDTRIP_2_FINDINGS.md). Encoded in tooling: profile `importGraphContract.editorRejectedDrawPairs` / `editorProvenDrawPairs` + the validator's `redraw-drawability` check — the shipped candidate now fails validation **by design** (11 errors + 7 warnings), and the regenerated `V6_REDRAW_CHECKLIST.md` carries a ⛔ halt banner (artifact bytes unchanged, SHA `0066192D…3A39`). **The candidate and checklist are not approved for further human testing. Gate: run the 5 editor drawability probes (findings §6), record results in the profile, then a human decision picks the rebuild architecture; only then is the generator rebuilt and a new candidate regenerated.** No interactive nodes may be inserted to bypass the editor warning without explicit operator UX approval.
- **Follow-up (2026-07-03) — corrected editor rule from the operator's live probe matrix; Condition-clone-cascade candidate shipped; gate is now round trip #3.** The probes **disproved the blanket Condition→Condition ban**: the editor rejects only a connection that would give a destination Condition a **second Condition-source parent** (corrected rule: *at most one Condition-source parent per Condition*); Condition→Condition (as first/only Condition-source parent), Condition→User Input Flow, Text→User Input Flow, Condition→Text, Text→Condition, and HTTP API→Condition are all **live-proven** operations (screenshots on record; [ROUNDTRIP_2_FINDINGS.md Addendum](../../artifacts/whatchimp/roundtrips/ROUNDTRIP_2_FINDINGS.md)). The operator directed the behavior-preserving **Condition-clone cascade** rebuild (no further architecture decision pending): every Condition that would receive an extra Condition-source parent got a guest-invisible per-parent clone whose single serialized connection the import keeps automatically — 16 clones (ids 750–765: `440`→750/752/754/756 + paired `602`→751/753/755/757, `470`→758/759/760, `660`→761, `663`→762, `690`→763/764/765). New canonical `Oraya_natural_intake_v6.txt`: **181 nodes, 214 output connections, 14 terminals, 15 declared merge points, exactly 34 operator-drawn connections** (27 Condition→User Input Flow, 2 Text→User Input Flow, 2 Text→Condition, 2 Condition→Text, 1 HTTP API→Condition, **zero drawn Condition→Condition**), SHA-256 `AB456A895221A46DE289EDA054DB9142B4D3F7D0A1892A3FFBEAFF999346AB0C`. Rules encoded: profile `editorProvenDrawPairs` (six ops) + `maxConditionSourceParents: 1`; validator `condition-parent-limit` check (graph-wide) + `redraw-drawability`; the generator refuses to emit any artifact whose redraw plan uses an unproven operation or leaves a Condition with two Condition-source parents. Evidence fixtures pinned byte-exact: round-trip-#1 pair + the halted 165-node candidate (`…roundtrip-2.halted-candidate.txt`, SHA `0066192D…3A39` — fails current validation with exactly the 11 beyond-limit Condition parents). Validator on the new candidate: exit 0, 0 errors, 0 warnings strict; simulator 50/50 (all 14 terminals); tooling tests 32/32. **Gate: round trip #3 — import → 34 redraws → save → close → reopen → export → comparator PRESERVED exit 0 + validator exit 0 on a fresh disposable bot** ([V6_ROUNDTRIP_CHECKLIST.md](../../artifacts/whatchimp/V6_ROUNDTRIP_CHECKLIST.md)).
- **Follow-up (2026-07-03) — round trip #3 HALTED: a Condition accepts at most ONE inbound connection TOTAL (any source type); full-cascade candidate shipped; gate is now round trip #4.** The operator imported the 181-node candidate on a fresh disposable bot; the open itself raised the infinite-loop warning (Text `#603` was left disconnected from Condition `#602`, which held only the import-kept `#440` edge), and drawing `#603 → #602` manually was rejected with the same warning — as were the other two Condition-destination items (`#659 → #654`, `#664 → #663`). Corrected rule: **`maxInboundPerCondition: 1` — no Condition may receive a second inbound edge, whatever the source type**; the round-trip-#2 probe matrix had only proven pairs against *unused* Condition inputs, which says nothing about already-connected destinations (probe-occupancy rule now standing policy). Evidence: [ROUNDTRIP_3_FINDINGS.md](../../artifacts/whatchimp/roundtrips/ROUNDTRIP_3_FINDINGS.md); the failed candidate is pinned byte-exact (`…roundtrip-3.failed-candidate.txt`, 141,728 bytes, SHA `AB456A89…AB0C` — fails current validation with exactly the 3 beyond-limit Condition inbound edges). The canonical artifact is rebuilt with the **full Condition-clone cascade** (+5 clones, ids 766–770; 21 clones total 750–770): every Condition has exactly one inbound edge and **no redraw targets a Condition**. New canonical `Oraya_natural_intake_v6.txt`: **186 nodes, 224 output connections, 14 terminals, 13 declared merge points (all User Input Flow wrappers or Texts), exactly 39 operator-drawn connections** (34 Condition→User Input Flow, 2 Text→User Input Flow, 3 Condition→Text), SHA-256 `1C7335ED49C7F9F7738B58CCDA4BA340543A9354002FAC2F9FBEC672784EDD48`. Rules encoded: profile `editorProvenDrawPairs` reduced to the 3 executed pairs + `maxInboundPerCondition: 1`; validator `condition-inbound-limit` (renamed from `condition-parent-limit`) + `redraw-drawability` now also errors if a declared hub is a Condition; the generator refuses to emit any Condition-targeting redraw. Validator on the new candidate: exit 0, 0 errors, 0 warnings strict; simulator 50/50 (all 14 terminals); tooling tests 33/33. **Gate: round trip #4 — import → 39 redraws → save → close → reopen → export → comparator PRESERVED exit 0 + validator exit 0 on a fresh disposable bot** ([V6_ROUNDTRIP_CHECKLIST.md](../../artifacts/whatchimp/V6_ROUNDTRIP_CHECKLIST.md)).
- **Follow-up (2026-07-04) — operator button evidence lands; INTERACTIVE-CONTROLS rebuild ships with ZERO operator redraws; gate is now the interactive acceptance procedure.** The operator's authenticated saved/reopened re-export (pinned byte-exact at [artifacts/whatchimp/roundtrips/Oraya_natural_intake_v6.button-evidence.saved-reexport.txt](../../artifacts/whatchimp/roundtrips/Oraya_natural_intake_v6.button-evidence.saved-reexport.txt), 148,882 bytes, SHA-256 `1D4A5E3DFC096F540037296D4E34551248D50F1F12BE64546B3F7E3B8A2C11EB`) proved Inline Buttons save directly to custom fields (`custom_57693`/`oraya_guest_count`, `new_post_back`) and **converge forward normally** after editor save/reopen — plus one hazard: button `#776` combined Start-a-Flow metadata (flow-id `value` + `postback_text` flow title) WITH a direct connector, the banned self-restart/double-execution construction (`#783` is the clean schema). The list-message schema (Interactive→Keyboard→Sections→Rows, rows writing custom fields, e.g. row `#47` → `custom_57698`/`oraya_villa`) was authenticated from a genuine tenant export. The canonical `Oraya_natural_intake_v6.txt` was **rebuilt on Interactive controls**: guest count = one-click 7-row list (1–6 + "More than 6"), bedrooms = 6 Interactives × 3 buttons (`custom_69114`), villa = 2 canonical buttons (`custom_57698`); extracted values skip their question; no confirmation/Edit/handoff-choice/happy-path name ask; completion = Lead Submit `6961` → summary; `7459` no longer referenced. **161 nodes, 211 output connections, 12 Interactive, 55 controls (35 Rows + 20 buttons), 10 terminals, ZERO operator redraws**, SHA-256 `F28106948012125CFB3324CEEDCDB414C8B2C0B33BF4B1179D827634550669E7`. Validator strict 0 errors/0 warnings (new `interactive-contract` check + postback-hub contract `{466:5, 860:30, 930:18, 938:2}` + Start-a-Flow ban); simulator 34/34 with full click coverage of every control label; tooling tests 35/35. **NEW unverified platform semantics this rebuild rests on (all human-gated):** (1) import survival of *serialized* postback convergence — the evidence only covers editor-drawn convergence surviving save/reopen; (2) stored value == visible label — the export schema has no separate value field on buttons/rows; (3) `rowOutput` forward routing — no genuine export in the 22-file corpus carries a Rows→next edge (that bot continued via `interactiveOutput`); (4) 30-way row fan-in scale on the guest acknowledgement Text; (5) live WhatsApp rendering of the 7-row list message; (6) free-text typed at an Interactive does NOT advance the flow — guest must tap (known UX gap, watch for stranded conversations).
- **Follow-up (2026-07-04) — round trip #4: NARROW failure isolates `Rows → User Input Flow` as an import-dropped class; repaired via one shared acknowledgement Text; control → Text convergence import survival is now PROVEN.** The operator ran the acceptance import on the 161-node candidate and saved/re-exported ([artifacts/whatchimp/roundtrips/Oraya_natural_intake_v6.roundtrip-4.saved-reexport.txt](../../artifacts/whatchimp/roundtrips/Oraya_natural_intake_v6.roundtrip-4.saved-reexport.txt), 140,217 bytes, SHA-256 `9C9EAD4C6D72B1B13193026ACF85182794ABFE3571FA17CB6E0ECEE788CFC031`). Result: all 161 nodes survived; **exactly 5 of 211 connections were dropped, all the same class — the five "More than 6" `rowOutput → #466 userInputFlowInput` edges** — leaving those rows with empty outputs and stranding the large-group subtree (`#466/#467/#468/#712–#715` unreachable). **Everything else survived intact, including all three control → Text postback hubs at full fan-in (30-way `#860`, 18-way `#930`, 2-way `#938`)** — so unknown (1) of the previous follow-up is answered for the control → Text class, and the evidence isolates the *target type* (`User Input Flow`), NOT postback convergence in general. Repair (smallest structural change): all five "More than 6" rows now converge on ONE shared acknowledgement Text `#865` ("Got it 😊"), which carries the single serialized connection into `#466`; the large-group exact-count → team review → lead → terminal path is byte-preserved. New canonical: **162 nodes, 212 output connections, 12 Interactive, 55 controls, 10 terminals, ZERO redraws**, SHA-256 `F846CBFA51FBA0452DB17376123C205FC6D73DFC1F553EA3CC40CC5B2974CB47`. Encoded: generator gate + validator `interactive-contract` reject any control → User Input Flow edge (profile `importDroppedControlTargets`); the validator also errors when a declared postback hub is missing from a re-export; regression tests pin the fixture bytes, the exact 5-edge loss signature, the repaired routing (5 rows → `#865` → `#466`), unchanged 1–6/bedroom/villa classes, zero unreachable, and the ten terminals (37/37). Remaining human-gated unknowns: the repaired five-connector class on a fresh import (expected to survive — it is now the proven control → Text class, but verify), stored-value==label, one-press-one-transition, live 7-row list rendering, and the free-text-at-Interactive UX gap.
- **Follow-up (2026-07-04) — dates-pending continuation replaces date escalation (behavior decision, not a platform finding).** After ONE date follow-up + ONE retry, a failed final retry now **continues into the normal interactive intake** instead of escalating: no name question, no date-escalation tail (nodes 640–643/700–703 deleted from the canonical graph); the lead submits with the date fields as the literal `"null"` (backend stores NO date — proven by `lib/butler/leads-absent-dates.test.mts`, 4/4), the secure prefill URL is minted from the lead id alone, and each completion Lead Submit feeds a single-inbound date-branch Condition selecting the **dated** summary (both dates shown) or the **undated** summary ("Dates: Please choose them using the secure link below." — no `null`, no date hashtags, no half-captured date; guest picks dates on `/book`). Large-group escalation behavior is unchanged. New canonical: **200 nodes, 270 output connections, 16 Interactive, 75 controls, 12 terminals, ZERO redraws**, SHA-256 `4D032FEC5209EF6685BD84BDA0980EBAAD7AB8BF8031AD9B8DD96BCB9BE1E46B`; hubs `{860:42, 865:7, 930:24, 938:2}` — note the guest-ack fan-in grew 30 → 42, beyond the 30-way scale round trip #4 proved (same class; the acceptance import verifies it). Validator strict 0/0 (new dated/undated summary contract + refine-API continuation checks), simulator 40/40 (D01–D07 dates-pending matrix), tooling tests 38/38.
- **Follow-up (2026-07-04) — first LIVE conversation failed on a runtime data contract (structural round trip PASSED: 200 nodes / 270 connections survived import+save+export).** Live transcript: "mechmech july 10 to july 11 for 4 people 2 bedrooms" → bot asked bedrooms → dated summary with **Check-in: 2026-07-10 but a BLANK Check-out line and a BLANK secure-link line**. Three compounding causes, all repaired: **(1) Backend extractor bug (reproduced + fixed):** a trailing bedroom phrase corrupted date parsing — "july 10 to july 11 for 4 people 2 bedrooms" extracted check-in only, and "july 10 for 4 people 2 bedrooms" extracted NO dates. The extractor now strips the bedroom phrase before date parsing and extracts it as `extracted.bedroom_count` / `extracted_text.bedroom_count` (exact approved labels or `"null"`); the live message now extracts both dates + villa + guests + bedrooms (regression tests in `lib/butler/extract-stay-intent.test.mts`, 41/41). **(2) Blank-value routing hole (repaired in the graph):** the tenant's response mapping was bound to `extracted.*`, whose JSON `null` WhatChimp wrote as `""` — and `""` passed every `= "null"` missing-check as if a date existed. All date/villa conditions now route on **PRESENCE** (`contains "-"` for dates, `contains "Villa"` for villa; `""`, `"null"`, whitespace, absent all read as missing), and the dated summary requires BOTH real dates (validator `date-presence` check; simulator blank-string/half-date/whitespace scenarios L01/B01–B03). Binding contract remains `extracted_text.*`. **Residual (documented):** the guest-count gates still use exact-value/`"null"` equality — a blank `oraya_guest_count` routes to the large-group review tail (safe human outcome), not a re-ask. **(3) Blank secure link:** the Lead Submit TEST integration lacked the `prefill_url` → `oraya_prefill_url` response mapping (or a `3xx`/missing `BUTLER_PREFILL_SECRET`) — backend behavior is proven (prefill minted from the lead id alone; `leads-absent-dates.test.mts`), and the checklist now carries an explicit blank-link troubleshooting order. The artifact also adopts the operator's hand-tuned **visual layout** (fixture `artifacts/whatchimp/roundtrips/Oraya_natural_intake_v6.roundtrip-5.layout-baseline.txt`, SHA-256 `7DDF81E4C2DA5EF9DA475D64E62B7989B0FEF1AEB154465D8050EACC2ADD3156`; positions only — that export's accidental Lead Submit rebinds to `7459`/`7460` are NOT adopted and are test-blocked). New canonical: **200 nodes / 270 connections / 12 terminals / ZERO redraws**, SHA-256 `5D39B6580A9AB784C31811D1255F4AD6509581F88635006FB6AECE7272A56B64`. Validator strict 0/0; simulator 44/44; tooling tests 39/39.
- **Follow-up (2026-07-08) — PR #67 merged and deployed; LIVE test PASSED except the bedroom question; bedroom-skip hotfix shipped (branch `claude/v6-bedroom-skip`).** The post-merge live conversation confirmed dates, villa, guests, lead persistence, and the secure prefill link all work; the one remaining gap was UX — the bedroom question still rendered when the guest had already volunteered the count ("… 2 bedrooms"). Each of the 8 bedroom-stage entries now feeds a **bedroom-known gate** (equality any-match on the three approved labels exactly; `""`/`"null"`/whitespace/invalid all fail → ask as before, preserving the stale-bedroom reset semantics); the skip side carries a branch-local villa gate + Lead Submit + summary tail (dated/undated hard-wired per branch reachability; only the clicked-guest branch keeps a date-branch Condition), and the 8 branch-local villa Interactives rejoin the shared villa-ack `#938` via their buttons — that hub grows **2-way → 18-way** (the proven control → Text class at a new scale; human-gated on import). The extractor already normalizes singular "2 bedroom" → "2 bedrooms" (new regression test). New canonical: **258 nodes / 344 connections / 24 Interactive / 91 controls / 21 terminals / ZERO redraws**, SHA-256 `8A356B6F508AF158274576521958F98FA2992A06560E23A65CB1558FBBB51A6D`; hubs `{860:42, 865:7, 930:24, 938:18}`; the 1000-range skip nodes are the only nodes outside the operator layout baseline (computed positions). Validator strict 0/0; simulator 62/62 (L01 = the live message with ZERO questions; K01–K17 skip matrix; T03 stale-bedroom re-ask); tooling tests 40/40; extractor 42/42.
- **Follow-up (2026-07-08) — structured Normalize Dates ladder replaces the "send both dates together" retry (branch `claude/v6-structured-date-fallback`).** Per David's hybrid-ladder clarification the whole v5.5 retry layer is gone; date recovery now saves the guest's raw words into `oraya_check_in_text` (58017) / `oraya_check_out_text` (58018) and calls the **existing tenant integration 7458 "Oraya Normalize Dates"** (fixed body reads exactly those two fields), with single-date questions (check-out-only, or two chained one-by-one) and — for a check-out-only gap — the follow-up `#426` REBOUND to 58018 and feeding ND FIRST (fixes the split-turn "11 july" reply the v5.5 refine-first wiring clobbered). Each outcome ends on a one-button "Continue" Interactive whose press rejoins the intake through the dates-recovered `#1150` (4-way) or dates-pending `#1200` (3-way) hub Text. The 258-node bedroom-skip artifact's import survival was RE-PROVEN at full scale — the TEST bot's re-export (pinned as round-trip-6 fixture, SHA `BEB37863…C707`) kept all 344 edges with the 42/24/18/7-way hubs intact — but the retry-layer deletion drops the bedroom-skip stages 8→5, so the new canonical is smaller. **NEW platform items this rests on (human-gated):** (a) integration 7458 still exists tenant-side (older flows referenced a superseded `7102`); (b) two ADDITIVE 7466/8101 response-mapping rows — `extracted_text.check_in`→`oraya_check_in_text`, `extracted_text.check_out`→`oraya_check_out_text` — so an already-extracted ISO date reaches the ND body; (c) the "Continue" → hub connectors surviving import (proven control → Text class). New canonical: **192 nodes / 247 connections / 22 Interactive / 62 controls / 15 terminals / ZERO redraws**, SHA-256 `4383A12F4EEE2E1E721F94BB03DA584B0383D3F47A69BC77C48F659C2E841659`; hubs `{860:24, 865:4, 930:15, 938:12, 1150:4, 1200:3}`. Validator strict 0/0 (new `normalize-dates-placement` check); simulator 53/53; tooling 42/42; new `lib/butler/normalize-dates.test.mts` 9/9 (backend UNTOUCHED); extractor 42/42.
- **Follow-up (2026-07-08) — backend extractor lead-in fix; PR #69 now DOES change backend code (extractor 42→48).** The operator tested the full sentence **"i want to book mechmech from july 10 to july 15 for 4 people 2 bedrooms"** against the Stay Intent API and got `check_in=null`/`check_out=null` (`status: unclear`) with villa/guests/bedrooms fine — so the presence gates passed STALE dates from a prior attempt into the dated summary. **Root cause (backend `lib/butler/extract-stay-intent.ts`, not the flow):** "want" is a filler word but the trailing "to" in "want **to**" is a range connective, not filler, so it survived filler-stripping as a stranded leading token; `splitStayResidue` then split on the WRONG "to" (`"to july 10 to july 15"` → check-in fragment `"to july 10"`), failing to parse and returning `unclear` with both dates null. The same class stranded "can" in "can i book …". **Fix (commit `0ad7dbf`):** `cleanResidue` drops leading lead-in tokens up to the first date-anchoring token (`DATE_ANCHOR_RE`); villa/guest-count are already extracted upstream, so the residue's content begins at its first date anchor and interior tokens (the real "to" separator) are untouched. Extractor tests **42→48** (6 new: `i want to book …`, `i'd like to book …`, `i want to stay at …`, `can i book …`, dates-only, spelled-number-duration guard); `tsc --noEmit` exit 0, `npm run build` exit 0; **flow artifact / generator / profile / validator / simulator / tooling UNCHANGED** (backend-only — no regenerate). The earlier "backend UNTOUCHED" note above applied to the ladder work only; it no longer holds for the branch as a whole. **Verification caveat:** WhatChimp TEST cannot reproduce the fix while pointed at production (`www.stayoraya.com` does not yet carry `0ad7dbf`) — point the TEST Stay Intent + Refine integrations at the PR's Vercel Preview (`https://oraya-bdmj34w73-staleen-2375s-projects.vercel.app`, or the branch alias `https://oraya-web-git-claude-v6-structure-fcfea9-staleen-2375s-projects.vercel.app`; both butler routes verified reachable with app-level `401`, no Vercel SSO wall) or merge PR #69 first. **Do NOT keep re-tuning the mapping for this full-message case until the endpoint under test contains the fix** — the two-target date mapping (each date → BOTH the final field and the `_text` mirror) is still mandatory regardless (`V6_DEPENDENCIES.md`).
- **Recommended fix path:** run the **interactive acceptance procedure** in [V6_ROUNDTRIP_CHECKLIST.md](../../artifacts/whatchimp/V6_ROUNDTRIP_CHECKLIST.md) (fresh disposable bot, import with NO warning and NO loose connectors — **inspect the "More than 6" connectors, the 12-way villa-ack fan-in, and the "Continue" → dates-recovered/dates-pending hub connectors explicitly**, click EVERY control and inspect all custom fields, exactly-once advance, save/close/reopen retest, export → comparator PRESERVED + strict validator exit 0), then live checklist section C — **C1 requires the exact live-failure message to complete with ZERO questions + both dates + a non-empty secure link; C1a the skip/ask matrix; C4 now covers the structured ladder (split-turn "11 july" first-try recovery, one-by-one, check-out retry, dates-pending undated summary).** Confirm integration `7458` exists tenant-side, add the two additive date-mirror 7466/8101 rows (plus `bedroom_count`), rebind the normalization mappings to `extracted_text.*`, and verify the Lead Submit `prefill_url` mapping before testing. **There is nothing to redraw.**
- **Discovered:** 2026-07-02 (Phase 16A v5.5 → v6 audit).

---

### #11 — Bare production API host answers WhatChimp POSTs with a 308 redirect that WhatChimp does not safely follow; production integrations must use the direct `www` API host

- **Severity:** 🟠 High until every production WhatChimp HTTP API integration is individually audited for the direct host.
- **Area:** WhatChimp HTTP API integrations / domain routing (tenant-level configuration, outside this repo)
- **Description:** operator-verified 2026-07-03 during an authenticated WhatChimp test. A production Lead Submit integration pointed at the bare `https://stayoraya.com` origin's `/api/butler/lead` path received an HTTP `308` redirect instead of the real endpoint response, and WhatChimp did not safely complete the redirected POST — the flow never received response fields such as `prefill_url`, so the secure website-handoff link stayed unavailable. After the operator switched the integration to the direct host `https://www.stayoraya.com/api/butler/lead`, WhatChimp reported success, Vercel recorded `POST /api/butler/lead` with HTTP `200`, the prefill secret was present, no prefill-token-generation failure was logged, and the response mapping visibly included `lead_id`, `message`, and `prefill_url` → `oraya_prefill_url`. **Contract:** production WhatChimp server-to-server HTTP API calls use `https://www.stayoraya.com/api/butler/...`; guest-facing links remain `https://stayoraya.com/...` (no change to canonical guest origin, prefill-link generation, website routing, or domain configuration). Any HTTP API verification receiving a `3xx` must be treated as a failure.
- **Status:** open — one Lead Submit integration corrected and verified; the other three production integrations (Stay Intent `7466`, Refine `8101`, and the remaining Lead Submit of `6961`/`7459`) must each be audited before production cutover (checklist D.2 in [artifacts/whatchimp/V6_ROUNDTRIP_CHECKLIST.md](../../artifacts/whatchimp/V6_ROUNDTRIP_CHECKLIST.md)). A tooling regression test now blocks operator docs from reinstructing the bare API prefix. Note the direct-host behavior depends on current domain routing; if routing/redirect configuration changes later, re-verify.
- **Recommended fix path:**
  1. Audit all four production integrations' endpoint hosts in the WhatChimp UI (A1.8 rule: only a direct `2xx` counts as success).
  2. Keep guest-facing copy and prefill links on `https://stayoraya.com` — this is not a domain migration.
  3. Optional future hardening (separate approved task): make the bare-host API paths answer directly instead of redirecting; until then the `www` host is the required WhatChimp target.
- **Discovered:** 2026-07-03 (authenticated WhatChimp Lead Submit test + Vercel logs).

---

### #12 — `resolveBookingByReference` never matched any reference: PostgREST rejects the `id::text` cast filter, and the error collapsed to "not_found"

- **Severity:** 🟠 High (was silent) — every guest-typed booking-reference lookup failed while looking like a clean "no such reference" answer.
- **Area:** `lib/booking-reference.ts` / `/api/butler/identify` reference branch / `/api/butler/booking-lookup` / `/api/butler/confirmed-guest-info` (Stage 4B)
- **Description:** `resolveBookingByReference` filtered with `.ilike("id::text", "<prefix>%")`. PostgREST does not accept a cast in a filter key, so the query errored server-side; by design the helper collapses lookup errors to `{ kind: "not_found" }` (guests must not be able to distinguish "lookup blew up" from "no match"), which made the failure invisible. Discovered during the Stage 4B PR #80 Preview verification (2026-07-15): a confirmed booking whose Arrival Guide and booking-view pages rendered fine (reference `0F28C563`) came back `ask_for_alternative_identifier` from `/api/butler/confirmed-guest-info`. The bug affects every consumer of the shared resolver — including the production Guest Identification v2 reference-fallback branch — meaning a guest who typed their reference has never successfully resolved through it; subscriber-id continuity paths were unaffected.
- **Status:** **closed (resolved 2026-07-15, PR #80)** — the lookup now uses a uuid **range** on the indexed primary key: the 8-hex reference is the uuid's first 4 bytes, so `gte <prefix>-0000… / lt <prefix+1>-0000…` (inclusive `lte` max-uuid for the `ffffffff` edge) covers exactly the prefix with plain, universally-supported operators. Pure range computation lives in [lib/booking-reference-range.ts](../../lib/booking-reference-range.ts) with a focused `node:test` suite (7/7). Behavior contract unchanged: normalize → not_found / ambiguous(≥2) / found, errors still collapse to not_found.
- **Recommended fix path:** n/a — resolved. Lesson recorded: PostgREST filter keys cannot carry `::` casts; error-collapsing resolvers need at least one live-path verification before being presumed working.
- **Discovered:** 2026-07-15 (PR #80 Preview human verification by David).
- **Resolved:** 2026-07-15

---

### #13 — Guest booking-status copy contradicted authoritative payment state

- **Severity:** 🟠 High (guest trust and payment-state clarity)
- **Area:** `/booking/view/[token]`, `/book`, and public hosted-checkout failures
- **Description:** after an authoritative CyberSource sandbox approval, a booking could correctly remain `PENDING` while `payment_status` became `paid_in_full`. The dedicated payment panel then showed "Payment received successfully" and "Paid in full", but a separate booking-status card still said "No payment required yet"; the confirmed variant separately coupled "Payment received / booking confirmed". The page also exposed the raw persisted method `card_manual`, pre-payment copy still implied payment only followed review, and some public checkout failure paths could echo provider/configuration detail. David found the contradiction during Preview verification after merged PR #81. The payment record itself was not forged by the browser return: the server had accepted the CyberSource response as `AUTHORIZED` or `CAPTURED`, while booking confirmation deliberately remained an Oraya operations decision.
- **Status:** **in-progress (2026-07-17 corrective branch)** — booking-status trust copy is payment-neutral; a pure guest-payment projection owns payment vocabulary and prioritizes recorded paid state over stale link state; payment method labels use the shared formatter; `/book` copy now describes pay-before-confirm correctly; and public checkout errors are guest-safe. Focused state-matrix coverage protects pending+paid, unpaid link states, and browser-return behavior.
- **Recommended fix path:** merge and Preview-verify the corrective PR. Keep the invariant that `bookings.status` and `payment_status` are independent; do not auto-confirm a paid booking. Separately obtain the official provider declined-card vector, because a sandbox test card authorizing is not evidence that decline handling is validated.
- **Discovered:** 2026-07-17 (David, PR #81 Vercel Preview screenshot and manual sandbox payment).

---

### #14 — Unified Checkout completion is not durably idempotent before provider authorization

- **Severity:** 🔴 Critical production blocker (production checkout remains disabled)
- **Area:** `/api/payments/unified-checkout-complete`, provider authorization, payment attempts
- **Description:** the completion route reads an `active` booking payment link, calls CyberSource authorization/capture, and only then updates the booking with `.eq("payment_link_status", "active")`. Two concurrent completion requests can both read `active` and both submit provider operations before either database update wins. The later conditional update can match zero rows without surfacing an error because matched-row count is not checked, yet the route still returns success. Session creation has the related orphan-session risk when its post-provider conditional update matches no row. There is no durable payment-attempt record, pre-authorization claim, or documented provider idempotency key tying a retry to one operation.
- **Status:** RESOLVED 2026-07-24 (Plan 3 Phase 3 PR) — durable payment-attempt ledger shipped. `payment_attempts` table (`sql/plan3-payment-attempts.sql`, human-run, partial unique index = at most one in-flight attempt per booking) is claimed atomically BEFORE the provider call; a concurrent second completion gets 409 without ever reaching CyberSource. A deterministic merchant reference derived from the attempt id (`lib/payments/unified-checkout-completion.ts` `deriveMerchantReference`) is sent as `clientReferenceInformation.code` and persisted with the provider transaction ids on the attempt row. Every conditional booking update (completion + session routes) is `.select("id")` row-count verified — an approved charge whose update matches zero rows marks the attempt `ambiguous`, logs `RECONCILIATION REQUIRED`, and returns an explicit error (never `ok: true`). Terminal states: `recorded` / `failed` (decline — releases the claim for retry) / `ambiguous` (timeout or zero-row — BLOCKS new attempts until manually reconciled; runbook in the SQL file). Code fails CLOSED (503) while the table is missing. Tests: `lib/payments/unified-checkout-completion.test.mts` (9). **Production checkout remains DISABLED — enabling it is a separate explicit decision after #15 is assessed.**
- **Note (2026-07-23, remediation 1.6):** the separate webhook-handler `set_paid` double-count (duplicate/concurrent webhook deliveries or a different-reference event re-adding `amount_paid`) is FIXED — `lib/payments/webhook-set-paid.ts` makes any already-paid link idempotent and the DB write is guarded with a NULL-safe not-paid filter + matched-row check. That fix is distinct from and does not close this completion-route idempotency gap.
- **Recommended fix path:** design a dedicated payment-attempt/idempotency contract before production activation: atomically claim a unique attempt before calling the provider, send a stable provider idempotency/request identifier where supported, persist provider request/transaction identifiers, verify every conditional update's returned row count, and test concurrent calls plus retry-after-timeout behavior. Reconcile ambiguous outcomes against CyberSource before allowing another charge.
- **Discovered:** 2026-07-17 (full payment-code audit after PR #81 Preview verification).

---

### #15 — Admin "Issue refund" records internal state but does not execute a provider refund

- **Severity:** 🟠 High operational/money-movement risk
- **Area:** admin booking payment actions / CyberSource refunds
- **Description:** the admin payment foundation exposes an "Issue refund" action, but the current implementation only updates booking refund/payment fields. It does not call CyberSource follow-on refund, void, or reversal APIs and does not verify provider acceptance, settlement state, or refunded amount. The UI is therefore a bookkeeping control, not proof that money returned to the guest.
- **Status:** **RESOLVED-BY-POLICY 2026-07-25 (Plan 4 Phase 1)** — refunds are MANUAL-FIRST by explicit decision (David, 2026-07-24): money moves only by hand in the NetCommerce Business Center, and the admin action is now honestly labeled **"Record manual refund"** ("Execute the refund in the NetCommerce Business Center first — this only records it"). Recording an executed refund REQUIRES the Business Center refund/transaction reference — `lib/payments/manual-refund.ts` validation in the admin bookings PATCH route returns 400 without it (tests: `lib/payments/manual-refund.test.mts`). The reference is stored in `bookings.refund_provider_reference` (`sql/plan4-refund-provider-reference.sql`, additive human-run; pre-migration the route retries without the column and the reference is preserved in `payment_notes`). Operator runbook: `docs/system/REFUND_RUNBOOK.md` (Business Center steps + ambiguous-attempt reconciliation in one doc). **Upgrade path:** automated provider-side refunds (role-gated CyberSource refund/void endpoints with idempotency and audit history) are a separate, later plan — this entry documents the honest manual baseline, not the end state.
- **Recommended fix path:** ~~before live charging, rename or clearly gate the current action as an internal record-only operation~~ (done, Plan 4 Phase 1), then — as the separate future plan — implement separately approved, role-gated provider refund/void endpoints with idempotency, transaction references, audit history, reconciliation, and explicit partial/full refund semantics. The locked admin route must not be edited without named approval.
- **Discovered:** 2026-07-17 (full payment-code audit; architectural gap was already described generally in the Phase 16B payment operations audit).

---

<!-- New entries go above this line, lowest # at the top. Closed entries can be moved to a "Closed" section below or stay in place with status: closed + date. -->

## Closed / wontfix

(none yet)
